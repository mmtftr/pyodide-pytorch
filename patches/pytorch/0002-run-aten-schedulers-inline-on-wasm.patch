From aab0fc20d88dedd689fcda63869a26c7847d174f Mon Sep 17 00:00:00 2001
From: pytorch-pyodide maintainers <noreply@github.com>
Date: Sun, 19 Jul 2026 11:45:46 +0300
Subject: [PATCH 2/4] Run ATen schedulers inline on threadless Wasm

Define a single-threaded Emscripten mode, report one worker, reject larger thread counts, and execute scheduled work synchronously.
---
 CMakeLists.txt                             |  4 ++++
 aten/src/ATen/ParallelNative.cpp           | 26 ++++++++++++++++++++++
 aten/src/ATen/ParallelThreadPoolNative.cpp | 15 +++++++++++++
 c10/core/thread_pool.cpp                   |  4 ++++
 torch/csrc/autograd/engine.cpp             | 11 +++++++--
 5 files changed, 58 insertions(+), 2 deletions(-)

diff --git a/CMakeLists.txt b/CMakeLists.txt
index a8b655d9..13776f1e 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -706,6 +706,10 @@ if(LINUX AND CPU_AARCH64)
   add_link_options_if_supported("--stub-group-size=0x2000000")
 endif()
 
+if(EMSCRIPTEN)
+  add_compile_definitions(PYTORCH_WASM_NO_THREADS=1)
+endif()
+
 # Set INTERN_BUILD_MOBILE for all mobile builds. Components that are not
 # applicable to mobile are disabled by this variable. Setting
 # `BUILD_PYTORCH_MOBILE_WITH_HOST_TOOLCHAIN` environment variable can force it
diff --git a/aten/src/ATen/ParallelNative.cpp b/aten/src/ATen/ParallelNative.cpp
index 805df08a..3c81c74e 100644
--- a/aten/src/ATen/ParallelNative.cpp
+++ b/aten/src/ATen/ParallelNative.cpp
@@ -213,6 +213,12 @@ void init_num_threads() {
 }
 
 void set_num_threads(int nthreads) {
+#if defined(PYTORCH_WASM_NO_THREADS)
+  TORCH_CHECK(
+      nthreads == 1,
+      "This PyTorch WebAssembly build is single-threaded; nthreads must be 1");
+  return;
+#else
 #ifndef C10_MOBILE
   TORCH_CHECK(nthreads > 0, "Expected positive number of threads");
   int no_value = NOT_SET;
@@ -236,9 +242,13 @@ void set_num_threads(int nthreads) {
   TORCH_INTERNAL_ASSERT(pool, "Invalid thread pool!");
   pool->set_thread_count(nthreads);
 #endif // C10_MOBILE
+#endif // PYTORCH_WASM_NO_THREADS
 }
 
 int get_num_threads() {
+#if defined(PYTORCH_WASM_NO_THREADS)
+  return 1;
+#else
   at::internal::lazy_init_num_threads();
 #ifndef C10_MOBILE
   // not initializing pool unnecessarily,
@@ -257,6 +267,7 @@ int get_num_threads() {
   TORCH_INTERNAL_ASSERT(pool, "Invalid thread pool!")
   return in_parallel_region() ? 1 /* current thread */ : pool->get_thread_count();
 #endif // C10_MOBILE
+#endif // PYTORCH_WASM_NO_THREADS
 }
 
 int get_thread_num() {
@@ -264,6 +275,9 @@ int get_thread_num() {
 }
 
 bool in_parallel_region() {
+#if defined(PYTORCH_WASM_NO_THREADS)
+  return in_parallel_region_;
+#else
 #ifndef C10_MOBILE
   return in_parallel_region_ || (
     num_intraop_threads.load() == CONSUMED &&
@@ -273,9 +287,13 @@ bool in_parallel_region() {
 #else
   return in_parallel_region_;
 #endif // C10_MOBILE
+#endif // PYTORCH_WASM_NO_THREADS
 }
 
 void intraop_launch(const std::function<void()>& func) {
+#if defined(PYTORCH_WASM_NO_THREADS)
+  func();
+#else
 #ifndef C10_MOBILE
   if (!in_parallel_region() && get_num_threads() > 1) {
     _get_intraop_pool().run(func);
@@ -288,10 +306,17 @@ void intraop_launch(const std::function<void()>& func) {
   // Task parallelism is not currently supported.
   func();
 #endif // C10_MOBILE
+#endif // PYTORCH_WASM_NO_THREADS
 }
 
 c10::intrusive_ptr<c10::ivalue::Future> intraop_launch_future(
     const std::function<void()>& func) {
+#if defined(PYTORCH_WASM_NO_THREADS)
+  auto future = c10::make_intrusive<c10::ivalue::Future>(c10::NoneType::get());
+  func();
+  future->markCompleted();
+  return future;
+#else
 #ifndef C10_MOBILE
   auto future = c10::make_intrusive<c10::ivalue::Future>(c10::NoneType::get());
   if (!in_parallel_region() && get_num_threads() > 1) {
@@ -314,6 +339,7 @@ c10::intrusive_ptr<c10::ivalue::Future> intraop_launch_future(
   future->markCompleted();
   return future;
 #endif // C10_MOBILE
+#endif // PYTORCH_WASM_NO_THREADS
 }
 
 } // namespace at
diff --git a/aten/src/ATen/ParallelThreadPoolNative.cpp b/aten/src/ATen/ParallelThreadPoolNative.cpp
index 5af4dfcc..a3e17b24 100644
--- a/aten/src/ATen/ParallelThreadPoolNative.cpp
+++ b/aten/src/ATen/ParallelThreadPoolNative.cpp
@@ -48,15 +48,25 @@ std::shared_ptr<TaskThreadPoolBase> create_c10_threadpool(
 C10_REGISTER_CREATOR(ThreadPoolRegistry, C10, create_c10_threadpool)
 
 void set_num_interop_threads(int nthreads) {
+#if defined(PYTORCH_WASM_NO_THREADS)
+  TORCH_CHECK(
+      nthreads == 1,
+      "This PyTorch WebAssembly build is single-threaded; nthreads must be 1");
+  return;
+#else
   TORCH_CHECK(nthreads > 0, "Expected positive number of threads");
 
   int no_value = NOT_SET;
   TORCH_CHECK(num_interop_threads.compare_exchange_strong(no_value, nthreads),
       "Error: cannot set number of interop threads after parallel work "
       "has started or set_num_interop_threads called");
+#endif
 }
 
 size_t get_num_interop_threads() {
+#if defined(PYTORCH_WASM_NO_THREADS)
+  return 1;
+#else
   at::internal::lazy_init_num_threads();
   int nthreads = num_interop_threads.load();
   if (nthreads > 0) {
@@ -67,15 +77,20 @@ size_t get_num_interop_threads() {
   } else {
     return get_pool().size();
   }
+#endif
 }
 
 namespace internal {
 void launch_no_thread_state(std::function<void()> fn) {
+#if defined(PYTORCH_WASM_NO_THREADS)
+  fn();
+#else
 #if AT_EXPERIMENTAL_SINGLE_THREAD_POOL
   intraop_launch(std::move(fn));
 #else
   get_pool().run(std::move(fn));
 #endif
+#endif
 }
 } // namespace internal
 
diff --git a/c10/core/thread_pool.cpp b/c10/core/thread_pool.cpp
index 7effe584..b7f13542 100644
--- a/c10/core/thread_pool.cpp
+++ b/c10/core/thread_pool.cpp
@@ -8,6 +8,9 @@
 namespace c10 {
 
 size_t TaskThreadPoolBase::defaultNumThreads() {
+#if defined(PYTORCH_WASM_NO_THREADS)
+  return 1;
+#else
   size_t num_threads = 0;
 #if !defined(__powerpc__) && !defined(__s390x__)
   if (cpuinfo_initialize()) {
@@ -28,6 +31,7 @@ size_t TaskThreadPoolBase::defaultNumThreads() {
     num_threads = 1;
   }
   return num_threads;
+#endif
 }
 
 ThreadPool::ThreadPool(
diff --git a/torch/csrc/autograd/engine.cpp b/torch/csrc/autograd/engine.cpp
index 2fd391e6..3b39c93a 100644
--- a/torch/csrc/autograd/engine.cpp
+++ b/torch/csrc/autograd/engine.cpp
@@ -58,7 +58,7 @@ static void forked_autograd_child() {
 
 // Should be called before unsafe for forks (thread pool) calls
 static void track_bad_autograd_forks() {
-#if !defined(WIN32)
+#if !defined(WIN32) && !defined(PYTORCH_WASM_NO_THREADS)
   static auto result [[maybe_unused]] =
       pthread_atfork(nullptr, nullptr, forked_autograd_child);
 #endif
@@ -1461,7 +1461,14 @@ c10::intrusive_ptr<at::ivalue::Future> Engine::execute_with_graph_task(
     queue->push(
         NodeTask(graph_task, std::move(graph_root), std::move(input_buffer)));
 
-    if (current_depth >= max_recursion_depth_) {
+    const bool use_reentrant_thread_pool =
+#if defined(PYTORCH_WASM_NO_THREADS)
+        false;
+#else
+        current_depth >= max_recursion_depth_;
+#endif
+
+    if (use_reentrant_thread_pool) {
       // See Note [Reentrant backwards]
       // If reached the max depth, switch to a different thread
       add_thread_pool_task(graph_task);
