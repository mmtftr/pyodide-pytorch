From 1237fe5a9af7818a85bb86a1f54fa9e5b3676c25 Mon Sep 17 00:00:00 2001
From: pytorch-pyodide maintainers <noreply@github.com>
Date: Fri, 31 Jul 2026 16:26:13 +0400
Subject: [PATCH 10/12] Add browser torch-webgpu eager kernel profile

---
 setup.py                                      |   1 +
 torch/CMakeLists.txt                          |  42 +
 torch/__init__.py                             |   3 +
 torch/csrc/webgpu/browser_ops.cpp             | 650 ++++++++++++++++
 .../webgpu/compat/core/webgpu_allocator.h     |  21 +
 .../csrc/webgpu/compat/core/webgpu_context.h  |  21 +
 torch/csrc/webgpu/emdawnwebgpu_bridge.cpp     | 871 ++++++++++++++++++++++
 torch/csrc/webgpu/webgpu_backend.cpp          | 474 ++++++++++++
 torch/csrc/webgpu/webgpu_context.cpp          |  44 ++
 torch/webgpu/__init__.py                      | 485 ++++++++++++
 10 files changed, 2612 insertions(+)
 create mode 100644 torch/csrc/webgpu/browser_ops.cpp
 create mode 100644 torch/csrc/webgpu/compat/core/webgpu_allocator.h
 create mode 100644 torch/csrc/webgpu/compat/core/webgpu_context.h
 create mode 100644 torch/csrc/webgpu/emdawnwebgpu_bridge.cpp
 create mode 100644 torch/csrc/webgpu/webgpu_backend.cpp
 create mode 100644 torch/csrc/webgpu/webgpu_context.cpp
 create mode 100644 torch/webgpu/__init__.py

diff --git a/setup.py b/setup.py
index 14d9fa3..21e2cd1 100644
--- a/setup.py
+++ b/setup.py
@@ -1231,6 +1231,7 @@ def main() -> None:
         "utils/model_dump/*.mjs",
         "_dynamo/graph_break_registry.json",
         "tools/dynamo/gb_id_mapping.py",
+        "webgpu/licenses/*.txt",
     ]
 
     if not BUILD_LIBTORCH_WHL:
diff --git a/torch/CMakeLists.txt b/torch/CMakeLists.txt
index 4587c64..36f6988 100644
--- a/torch/CMakeLists.txt
+++ b/torch/CMakeLists.txt
@@ -39,6 +39,38 @@ set(TORCH_PYTHON_SRCS
     ${GENERATED_CXX_PYTHON}
     )
 append_filelist("libtorch_python_core_sources" TORCH_PYTHON_SRCS)
+if(EMSCRIPTEN)
+  list(APPEND TORCH_PYTHON_SRCS
+    ${TORCH_SRC_DIR}/csrc/webgpu/webgpu_backend.cpp
+    ${TORCH_SRC_DIR}/csrc/webgpu/webgpu_context.cpp
+    ${TORCH_SRC_DIR}/csrc/webgpu/emdawnwebgpu_bridge.cpp
+    ${TORCH_SRC_DIR}/csrc/webgpu/browser_ops.cpp
+    ${TORCH_ROOT}/third_party/torch-webgpu/csrc/ops/binary.cpp
+    ${TORCH_ROOT}/third_party/torch-webgpu/csrc/ops/unary.cpp
+    ${TORCH_ROOT}/third_party/torch-webgpu/csrc/ops/activation_functions.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/browser_unary.cpp
+    ${TORCH_ROOT}/third_party/torch-webgpu/csrc/ops/scalar_unary.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/argmax.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/boolean.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/creation.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/copy.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/embedding.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/gemma_rms_norm.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/generation_control.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/generation_long.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/long_arithmetic.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/kv_cache.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/matmul.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/masking.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/masked_fill.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/q8_linear.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/normalization.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/reduction.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/scalar_binary.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/swiglu.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/type_conversion.cpp
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels/attention.cpp)
+endif()
 
 # NB: This has to match the condition under which the JIT test directory
 #     is included (at the time of writing that's in caffe2/CMakeLists.txt).
@@ -76,6 +103,16 @@ set(TORCH_PYTHON_INCLUDE_DIRECTORIES
     ${TORCH_SRC_DIR}/standalone
     )
 
+if(EMSCRIPTEN)
+  list(PREPEND TORCH_PYTHON_INCLUDE_DIRECTORIES
+    ${TORCH_SRC_DIR}/csrc/webgpu/compat
+    ${TORCH_ROOT}/third_party/torch-webgpu/csrc
+    ${TORCH_ROOT}/third_party/torch-webgpu/csrc/ops
+    ${TORCH_ROOT}/third_party/pyodide-pytorch-webgpu/llm_kernels
+    ${TORCH_ROOT}/third_party/emdawnwebgpu/webgpu/include
+    ${TORCH_ROOT}/third_party/emdawnwebgpu/webgpu_cpp/include)
+endif()
+
 list(APPEND TORCH_PYTHON_INCLUDE_DIRECTORIES ${LIBSHM_SRCDIR})
 
 set(TORCH_PYTHON_LINK_LIBRARIES
diff --git a/torch/__init__.py b/torch/__init__.py
index 10d44e6..addb63b 100644
--- a/torch/__init__.py
+++ b/torch/__init__.py
@@ -3081,6 +3081,9 @@ def _as_tensor_fullprec(t):
 if _is_device_backend_autoload_enabled():
     _import_device_backends()
 
+if sys.platform == "emscripten":
+    from torch import webgpu as webgpu
+
 from torch import _logging
 
 
diff --git a/torch/csrc/webgpu/browser_ops.cpp b/torch/csrc/webgpu/browser_ops.cpp
new file mode 100644
index 0000000..3ee45d7
--- /dev/null
+++ b/torch/csrc/webgpu/browser_ops.cpp
@@ -0,0 +1,650 @@
+// Browser-safe registrations built from torch-webgpu's existing C++ dispatch
+// helpers and WGSL. This file intentionally omits every upstream registration
+// that can synchronously read back or invoke a CPU fallback.
+
+#include <ATen/ATen.h>
+#include <ATen/TensorIterator.h>
+#include <ATen/core/grad_mode.h>
+#include <torch/library.h>
+#include <webgpu/webgpu_cpp.h>
+
+#include "binary.h"
+#include "core/webgpu_allocator.h"
+#include "core/webgpu_context.h"
+#include "generation_control.h"
+#include "llm_common.h"
+#include "long_arithmetic.h"
+
+#include <algorithm>
+#include <cstdint>
+#include <limits>
+#include <optional>
+#include <string>
+#include <utility>
+#include <vector>
+
+namespace torch_webgpu::browser_ops {
+namespace {
+
+using torch_webgpu::core::WebGPUAllocation;
+using torch_webgpu::ops::BinaryOp;
+
+void check_float_webgpu(const at::Tensor& tensor, const char* operation) {
+  TORCH_CHECK(
+      tensor.device().is_privateuseone(), operation, " expects a WebGPU tensor");
+  TORCH_CHECK(
+      tensor.scalar_type() == at::kFloat,
+      operation,
+      " supports only torch.float32");
+  TORCH_CHECK(
+      !at::GradMode::is_enabled() || !tensor.requires_grad(),
+      "WebGPU autograd is not supported; use `torch.no_grad()` for inference");
+}
+
+std::uint32_t checked_u32(std::int64_t value, const char* description) {
+  TORCH_CHECK(
+      value >= 0 &&
+          static_cast<std::uint64_t>(value) <=
+              std::numeric_limits<std::uint32_t>::max(),
+      description,
+      " does not fit in uint32 WebGPU metadata");
+  return static_cast<std::uint32_t>(value);
+}
+
+std::optional<at::Scalar> cpu_scalar_value(const at::Tensor& tensor) {
+  if (!tensor.device().is_cpu() || tensor.layout() != at::kStrided ||
+      tensor.dim() != 0 || tensor.numel() != 1) {
+    return std::nullopt;
+  }
+  return tensor.item();
+}
+
+template <BinaryOp Op>
+at::Tensor& binary_out(
+    const at::Tensor& lhs,
+    const at::Tensor& rhs,
+    const at::Scalar& alpha,
+    at::Tensor& output) {
+  check_float_webgpu(lhs, "WebGPU binary operation");
+  check_float_webgpu(rhs, "WebGPU binary operation");
+  check_float_webgpu(output, "WebGPU binary output");
+  TORCH_CHECK(lhs.device() == rhs.device() && lhs.device() == output.device());
+
+  at::TensorIteratorConfig config;
+  config.set_check_mem_overlap(true);
+  config.add_output(output);
+  config.add_input(lhs);
+  config.add_input(rhs);
+  config.check_all_same_dtype(true);
+  config.check_all_same_device(true);
+  auto iterator = config.build();
+  if (iterator.numel() == 0) {
+    return output;
+  }
+
+  const auto output_buffer = static_cast<WebGPUAllocation*>(
+      iterator.tensor(0).storage().data_ptr().get())->buffer.Get();
+  const auto lhs_buffer = static_cast<WebGPUAllocation*>(
+      iterator.tensor(1).storage().data_ptr().get())->buffer.Get();
+  const auto rhs_buffer = static_cast<WebGPUAllocation*>(
+      iterator.tensor(2).storage().data_ptr().get())->buffer.Get();
+  if (output_buffer == lhs_buffer || output_buffer == rhs_buffer) {
+    // WebGPU rejects a GPUBuffer bound as both read-only input and writable
+    // output in one pass. TensorIterator deliberately permits exact in-place
+    // overlap, so compute into distinct storage and copy back on the GPU.
+    auto temporary = at::empty(
+        output.sizes(), output.options(), c10::MemoryFormat::Contiguous);
+    at::TensorIteratorConfig temporary_config;
+    temporary_config.set_check_mem_overlap(true);
+    temporary_config.add_output(temporary);
+    temporary_config.add_input(lhs);
+    temporary_config.add_input(rhs);
+    temporary_config.check_all_same_dtype(true);
+    temporary_config.check_all_same_device(true);
+    auto temporary_iterator = temporary_config.build();
+    torch_webgpu::ops::run_binary_kernel<Op>(temporary_iterator, alpha);
+    pyodide_pytorch::webgpu::llm::copy_strided(temporary, output);
+    return output;
+  }
+  torch_webgpu::ops::run_binary_kernel<Op>(iterator, alpha);
+  return output;
+}
+
+template <BinaryOp Op>
+at::Tensor binary_tensor(
+    const at::Tensor& lhs,
+    const at::Tensor& rhs,
+    const at::Scalar& alpha = 1) {
+  check_float_webgpu(lhs, "WebGPU binary operation");
+  check_float_webgpu(rhs, "WebGPU binary operation");
+  TORCH_CHECK(
+      lhs.device() == rhs.device(),
+      "WebGPU binary operations do not implicitly transfer CPU tensors");
+  auto output = at::empty(at::infer_size(lhs.sizes(), rhs.sizes()), lhs.options());
+  if (output.numel() == 0) {
+    return output;
+  }
+  binary_out<Op>(lhs, rhs, alpha, output);
+  return output;
+}
+
+at::Tensor add_tensor(
+    const at::Tensor& lhs,
+    const at::Tensor& rhs,
+    const at::Scalar& alpha) {
+  if (lhs.device().is_privateuseone()) {
+    if (const auto scalar = cpu_scalar_value(rhs)) {
+      return at::add(lhs, *scalar, alpha);
+    }
+  }
+  if (rhs.device().is_privateuseone()) {
+    if (const auto scalar = cpu_scalar_value(lhs)) {
+      return at::add(at::mul(rhs, alpha), *scalar);
+    }
+  }
+  if (lhs.scalar_type() == at::kLong && rhs.scalar_type() == at::kLong) {
+    return pyodide_pytorch::webgpu::llm::add_long_tensor(lhs, rhs, alpha);
+  }
+  return binary_tensor<BinaryOp::Add>(lhs, rhs, alpha);
+}
+
+at::Tensor& add_out(
+    const at::Tensor& lhs,
+    const at::Tensor& rhs,
+    const at::Scalar& alpha,
+    at::Tensor& output) {
+  if (lhs.scalar_type() == at::kLong && rhs.scalar_type() == at::kLong &&
+      output.scalar_type() == at::kLong) {
+    auto result =
+        pyodide_pytorch::webgpu::llm::add_long_tensor(lhs, rhs, alpha);
+    pyodide_pytorch::webgpu::llm::copy_strided(result, output);
+    return output;
+  }
+  return binary_out<BinaryOp::Add>(lhs, rhs, alpha, output);
+}
+
+at::Tensor mul_tensor(const at::Tensor& lhs, const at::Tensor& rhs) {
+  if (lhs.device().is_privateuseone()) {
+    if (const auto scalar = cpu_scalar_value(rhs)) {
+      return at::mul(lhs, *scalar);
+    }
+  }
+  if (rhs.device().is_privateuseone()) {
+    if (const auto scalar = cpu_scalar_value(lhs)) {
+      return at::mul(rhs, *scalar);
+    }
+  }
+  if (lhs.scalar_type() == at::kBool || rhs.scalar_type() == at::kBool) {
+    return pyodide_pytorch::webgpu::llm::mul_bool_tensor(lhs, rhs);
+  }
+  if (lhs.scalar_type() == at::kLong && rhs.scalar_type() == at::kLong) {
+    return pyodide_pytorch::webgpu::llm::mul_long_tensor(lhs, rhs);
+  }
+  if ((lhs.scalar_type() == at::kFloat &&
+       (rhs.scalar_type() == at::kInt || rhs.scalar_type() == at::kLong)) ||
+      (rhs.scalar_type() == at::kFloat &&
+       (lhs.scalar_type() == at::kInt || lhs.scalar_type() == at::kLong))) {
+    return pyodide_pytorch::webgpu::llm::mul_float_integer_tensor(lhs, rhs);
+  }
+  return binary_tensor<BinaryOp::Mul>(lhs, rhs);
+}
+
+at::Tensor& mul_out(
+    const at::Tensor& lhs,
+    const at::Tensor& rhs,
+    at::Tensor& output) {
+  if ((lhs.scalar_type() == at::kLong && rhs.scalar_type() == at::kLong) ||
+      (output.scalar_type() == at::kFloat &&
+       ((lhs.scalar_type() == at::kFloat &&
+         (rhs.scalar_type() == at::kInt || rhs.scalar_type() == at::kLong)) ||
+        (rhs.scalar_type() == at::kFloat &&
+         (lhs.scalar_type() == at::kInt || lhs.scalar_type() == at::kLong))))) {
+    auto result = mul_tensor(lhs, rhs);
+    pyodide_pytorch::webgpu::llm::copy_strided(result, output);
+    return output;
+  }
+  return binary_out<BinaryOp::Mul>(lhs, rhs, 1, output);
+}
+
+at::Tensor sub_tensor(
+    const at::Tensor& lhs,
+    const at::Tensor& rhs,
+    const at::Scalar& alpha) {
+  if (lhs.device().is_privateuseone()) {
+    if (const auto scalar = cpu_scalar_value(rhs)) {
+      return at::sub(lhs, *scalar, alpha);
+    }
+  }
+  if (rhs.device().is_privateuseone()) {
+    if (const auto scalar = cpu_scalar_value(lhs)) {
+      return at::rsub(rhs, *scalar, alpha);
+    }
+  }
+  if (lhs.scalar_type() == at::kLong && rhs.scalar_type() == at::kLong) {
+    return pyodide_pytorch::webgpu::llm::sub_long_tensor(lhs, rhs, alpha);
+  }
+  return binary_tensor<BinaryOp::Sub>(lhs, rhs, alpha);
+}
+
+at::Tensor& sub_out(
+    const at::Tensor& lhs,
+    const at::Tensor& rhs,
+    const at::Scalar& alpha,
+    at::Tensor& output) {
+  if (lhs.scalar_type() == at::kLong && rhs.scalar_type() == at::kLong &&
+      output.scalar_type() == at::kLong) {
+    auto result =
+        pyodide_pytorch::webgpu::llm::sub_long_tensor(lhs, rhs, alpha);
+    pyodide_pytorch::webgpu::llm::copy_strided(result, output);
+    return output;
+  }
+  return binary_out<BinaryOp::Sub>(lhs, rhs, alpha, output);
+}
+
+at::Tensor div_tensor(const at::Tensor& lhs, const at::Tensor& rhs) {
+  if (lhs.device().is_privateuseone()) {
+    if (const auto scalar = cpu_scalar_value(rhs)) {
+      return at::div(lhs, *scalar);
+    }
+  }
+  if (rhs.device().is_privateuseone() && rhs.scalar_type() == at::kFloat) {
+    if (const auto scalar = cpu_scalar_value(lhs)) {
+      return at::mul(at::reciprocal(rhs), *scalar);
+    }
+  }
+  return binary_tensor<BinaryOp::Div>(lhs, rhs);
+}
+
+at::Tensor& div_out(
+    const at::Tensor& lhs,
+    const at::Tensor& rhs,
+    at::Tensor& output) {
+  return binary_out<BinaryOp::Div>(lhs, rhs, 1, output);
+}
+
+struct ComputeKernel {
+  wgpu::BindGroupLayout bind_group_layout;
+  wgpu::ComputePipeline pipeline;
+};
+
+ComputeKernel make_kernel(
+    const char* label,
+    const std::string& shader,
+    const std::vector<wgpu::BufferBindingType>& binding_types) {
+  wgpu::ShaderSourceWGSL shader_source{
+      wgpu::ShaderSourceWGSL::Init{
+          nullptr,
+          wgpu::StringView{shader.c_str(), shader.size()}}};
+  wgpu::ShaderModuleDescriptor shader_descriptor{};
+  shader_descriptor.nextInChain = &shader_source;
+  shader_descriptor.label = label;
+  auto& context = torch_webgpu::core::getWebGPUContext();
+  auto shader_module =
+      context.getDevice().CreateShaderModule(&shader_descriptor);
+
+  std::vector<wgpu::BindGroupLayoutEntry> entries(binding_types.size());
+  for (std::size_t index = 0; index < binding_types.size(); ++index) {
+    entries[index].binding = static_cast<std::uint32_t>(index);
+    entries[index].visibility = wgpu::ShaderStage::Compute;
+    entries[index].buffer.type = binding_types[index];
+    entries[index].buffer.hasDynamicOffset = false;
+    entries[index].buffer.minBindingSize = 0;
+  }
+  wgpu::BindGroupLayoutDescriptor layout_descriptor{};
+  layout_descriptor.entryCount = entries.size();
+  layout_descriptor.entries = entries.data();
+  auto bind_group_layout =
+      context.getDevice().CreateBindGroupLayout(&layout_descriptor);
+
+  wgpu::PipelineLayoutDescriptor pipeline_layout_descriptor{};
+  pipeline_layout_descriptor.bindGroupLayoutCount = 1;
+  pipeline_layout_descriptor.bindGroupLayouts = &bind_group_layout;
+  auto pipeline_layout =
+      context.getDevice().CreatePipelineLayout(&pipeline_layout_descriptor);
+
+  wgpu::ComputePipelineDescriptor pipeline_descriptor{};
+  pipeline_descriptor.layout = pipeline_layout;
+  pipeline_descriptor.compute.module = shader_module;
+  pipeline_descriptor.compute.entryPoint = wgpu::StringView{"main", 4};
+  auto pipeline =
+      context.getDevice().CreateComputePipeline(&pipeline_descriptor);
+  TORCH_CHECK(
+      bind_group_layout != nullptr && pipeline != nullptr,
+      "failed to create ",
+      label,
+      " WebGPU pipeline");
+  return {std::move(bind_group_layout), std::move(pipeline)};
+}
+
+const std::string& mm_shader() {
+  // Verbatim executable WGSL from torch-webgpu csrc/shaders/mm.wgsl at
+  // a4369ff0f61f4e58cbffb048cee85047b33dacba (comments omitted).
+  static const std::string shader = R"wgsl(
+const MAX_DIMS: u32 = 8u;
+
+struct Params {
+    M: u32,
+    N: u32,
+    K: u32,
+    _pad: u32,
+    self_offset: u32,
+    mat2_offset: u32,
+    out_offset: u32,
+    _pad2: u32,
+    self_strides: array<u32, MAX_DIMS>,
+    mat2_strides: array<u32, MAX_DIMS>,
+    out_strides: array<u32, MAX_DIMS>,
+};
+
+@group(0) @binding(0) var<storage, read> A: array<f32>;
+@group(0) @binding(1) var<storage, read> B: array<f32>;
+@group(0) @binding(2) var<storage, read_write> C: array<f32>;
+@group(0) @binding(3) var<uniform> params: Params;
+
+const wsx: u32 = 16u;
+const wsy: u32 = 16u;
+const wsz: u32 = 1u;
+
+@compute @workgroup_size(wsx, wsy)
+fn main(
+    @builtin(workgroup_id) wid: vec3<u32>,
+    @builtin(local_invocation_index) li: u32,
+    @builtin(num_workgroups) nwg: vec3<u32>) {
+    let workgroup_index = wid.x + nwg.x * wid.y + nwg.x * nwg.y * wid.z;
+    let index = workgroup_index * (wsx * wsy * wsz) + li;
+    if (index >= params.M * params.K) { return; }
+    let row = index / params.K;
+    let column = index % params.K;
+    var output: f32 = 0.0;
+    for (var i: u32 = 0u; i < params.N; i++) {
+        let a = params.self_offset + row * params.self_strides[0] +
+            i * params.self_strides[1];
+        let b = params.mat2_offset + i * params.mat2_strides[0] +
+            column * params.mat2_strides[1];
+        output += A[a] * B[b];
+    }
+    C[params.out_offset + index] = output;
+}
+)wgsl";
+  return shader;
+}
+
+ComputeKernel& mm_kernel() {
+  static ComputeKernel kernel = make_kernel(
+      "torch-webgpu mm",
+      mm_shader(),
+      {wgpu::BufferBindingType::ReadOnlyStorage,
+       wgpu::BufferBindingType::ReadOnlyStorage,
+       wgpu::BufferBindingType::Storage,
+       wgpu::BufferBindingType::Uniform});
+  return kernel;
+}
+
+at::Tensor& mm_out(
+    const at::Tensor& lhs,
+    const at::Tensor& rhs,
+    at::Tensor& output) {
+  check_float_webgpu(lhs, "WebGPU mm");
+  check_float_webgpu(rhs, "WebGPU mm");
+  check_float_webgpu(output, "WebGPU mm output");
+  TORCH_CHECK(lhs.dim() == 2 && rhs.dim() == 2, "WebGPU mm expects matrices");
+  TORCH_CHECK(lhs.size(1) == rhs.size(0), "WebGPU mm shape mismatch");
+  TORCH_CHECK(
+      output.dim() == 2 && output.size(0) == lhs.size(0) &&
+          output.size(1) == rhs.size(1),
+      "WebGPU mm output shape mismatch");
+  TORCH_CHECK(output.is_contiguous(), "WebGPU mm output must be contiguous");
+  if (output.numel() == 0) {
+    return output;
+  }
+
+  struct Params {
+    std::uint32_t m;
+    std::uint32_t n;
+    std::uint32_t k;
+    std::uint32_t pad;
+    std::uint32_t lhs_offset;
+    std::uint32_t rhs_offset;
+    std::uint32_t output_offset;
+    std::uint32_t pad2;
+    std::uint32_t lhs_strides[8];
+    std::uint32_t rhs_strides[8];
+    std::uint32_t output_strides[8];
+  };
+  static_assert(sizeof(Params) == 128);
+  Params params{};
+  params.m = checked_u32(lhs.size(0), "mm M");
+  params.n = checked_u32(lhs.size(1), "mm N");
+  params.k = checked_u32(rhs.size(1), "mm K");
+  params.lhs_offset = checked_u32(lhs.storage_offset(), "mm lhs offset");
+  params.rhs_offset = checked_u32(rhs.storage_offset(), "mm rhs offset");
+  params.output_offset =
+      checked_u32(output.storage_offset(), "mm output offset");
+  params.lhs_strides[0] = checked_u32(lhs.stride(0), "mm lhs stride 0");
+  params.lhs_strides[1] = checked_u32(lhs.stride(1), "mm lhs stride 1");
+  params.rhs_strides[0] = checked_u32(rhs.stride(0), "mm rhs stride 0");
+  params.rhs_strides[1] = checked_u32(rhs.stride(1), "mm rhs stride 1");
+  params.output_strides[0] =
+      checked_u32(output.stride(0), "mm output stride 0");
+  params.output_strides[1] =
+      checked_u32(output.stride(1), "mm output stride 1");
+
+  auto* lhs_allocation =
+      static_cast<WebGPUAllocation*>(lhs.storage().data_ptr().get());
+  auto* rhs_allocation =
+      static_cast<WebGPUAllocation*>(rhs.storage().data_ptr().get());
+  auto* output_allocation =
+      static_cast<WebGPUAllocation*>(output.storage().data_ptr().get());
+  TORCH_CHECK(lhs_allocation && rhs_allocation && output_allocation);
+
+  auto& context = torch_webgpu::core::getWebGPUContext();
+  wgpu::BufferDescriptor uniform_descriptor{};
+  uniform_descriptor.label = "torch-webgpu mm params";
+  uniform_descriptor.usage =
+      wgpu::BufferUsage::Uniform | wgpu::BufferUsage::CopyDst;
+  uniform_descriptor.size = sizeof(Params);
+  auto params_buffer =
+      context.getDevice().CreateBuffer(&uniform_descriptor);
+  context.getQueue().WriteBuffer(params_buffer, 0, &params, sizeof(params));
+
+  auto& kernel = mm_kernel();
+  wgpu::BindGroupEntry entries[4]{};
+  entries[0].binding = 0;
+  entries[0].buffer = lhs_allocation->buffer;
+  entries[0].size = lhs_allocation->buffer.GetSize();
+  entries[1].binding = 1;
+  entries[1].buffer = rhs_allocation->buffer;
+  entries[1].size = rhs_allocation->buffer.GetSize();
+  entries[2].binding = 2;
+  entries[2].buffer = output_allocation->buffer;
+  entries[2].size = output_allocation->buffer.GetSize();
+  entries[3].binding = 3;
+  entries[3].buffer = params_buffer;
+  entries[3].size = sizeof(Params);
+  wgpu::BindGroupDescriptor bind_group_descriptor{};
+  bind_group_descriptor.layout = kernel.bind_group_layout;
+  bind_group_descriptor.entryCount = 4;
+  bind_group_descriptor.entries = entries;
+  auto bind_group =
+      context.getDevice().CreateBindGroup(&bind_group_descriptor);
+
+  auto encoder = context.getDevice().CreateCommandEncoder();
+  auto pass = encoder.BeginComputePass();
+  pass.SetPipeline(kernel.pipeline);
+  pass.SetBindGroup(0, bind_group);
+  pass.DispatchWorkgroups((params.m + 15) / 16, (params.k + 15) / 16, 1);
+  pass.End();
+  auto command_buffer = encoder.Finish();
+  context.getQueue().Submit(1, &command_buffer);
+  return output;
+}
+
+at::Tensor mm(const at::Tensor& lhs, const at::Tensor& rhs) {
+  check_float_webgpu(lhs, "WebGPU mm");
+  check_float_webgpu(rhs, "WebGPU mm");
+  TORCH_CHECK(lhs.dim() == 2 && rhs.dim() == 2, "WebGPU mm expects matrices");
+  TORCH_CHECK(lhs.size(1) == rhs.size(0), "WebGPU mm shape mismatch");
+  auto output = at::empty({lhs.size(0), rhs.size(1)}, lhs.options());
+  return mm_out(lhs, rhs, output);
+}
+
+const std::string& softmax_shader() {
+  // Executable WGSL from torch-webgpu csrc/ops/softmax.cpp at the pinned
+  // commit, retained as a single-invocation-per-row reference kernel.
+  static const std::string shader = R"wgsl(
+struct Params {
+    batch_size: u32,
+    dim_size: u32,
+    self_offset: u32,
+    out_offset: u32,
+};
+@group(0) @binding(0) var<storage, read> input: array<f32>;
+@group(0) @binding(1) var<storage, read_write> output: array<f32>;
+@group(0) @binding(2) var<uniform> params: Params;
+
+@compute @workgroup_size(64)
+fn main(@builtin(global_invocation_id) gid: vec3<u32>) {
+    let batch = gid.x;
+    if (batch >= params.batch_size) { return; }
+    let input_base = params.self_offset + batch * params.dim_size;
+    let output_base = params.out_offset + batch * params.dim_size;
+    var maximum = input[input_base];
+    for (var i: u32 = 1u; i < params.dim_size; i++) {
+        maximum = max(maximum, input[input_base + i]);
+    }
+    var sum: f32 = 0.0;
+    for (var i: u32 = 0u; i < params.dim_size; i++) {
+        let value = exp(input[input_base + i] - maximum);
+        output[output_base + i] = value;
+        sum += value;
+    }
+    for (var i: u32 = 0u; i < params.dim_size; i++) {
+        output[output_base + i] /= sum;
+    }
+}
+)wgsl";
+  return shader;
+}
+
+ComputeKernel& softmax_kernel() {
+  static ComputeKernel kernel = make_kernel(
+      "torch-webgpu softmax",
+      softmax_shader(),
+      {wgpu::BufferBindingType::ReadOnlyStorage,
+       wgpu::BufferBindingType::Storage,
+       wgpu::BufferBindingType::Uniform});
+  return kernel;
+}
+
+at::Tensor browser_softmax(
+    const at::Tensor& input,
+    std::int64_t dim,
+    std::optional<at::ScalarType> dtype) {
+  check_float_webgpu(input, "WebGPU softmax");
+  if (dim < 0) {
+    dim += input.dim();
+  }
+  TORCH_CHECK(input.dim() > 0 && dim == input.dim() - 1,
+      "browser WebGPU softmax currently supports only the last dimension");
+  TORCH_CHECK(input.is_contiguous(),
+      "browser WebGPU softmax requires contiguous input");
+  TORCH_CHECK(!dtype || *dtype == at::kFloat,
+      "browser WebGPU softmax supports only float32 output");
+  auto output = at::empty_like(input);
+  if (input.numel() == 0) {
+    return output;
+  }
+  TORCH_CHECK(input.size(-1) > 0, "softmax dimension must be non-empty");
+
+  struct Params {
+    std::uint32_t batch_size;
+    std::uint32_t dim_size;
+    std::uint32_t input_offset;
+    std::uint32_t output_offset;
+  };
+  Params params{
+      checked_u32(input.numel() / input.size(-1), "softmax batch size"),
+      checked_u32(input.size(-1), "softmax dimension size"),
+      checked_u32(input.storage_offset(), "softmax input offset"),
+      checked_u32(output.storage_offset(), "softmax output offset")};
+  auto* input_allocation =
+      static_cast<WebGPUAllocation*>(input.storage().data_ptr().get());
+  auto* output_allocation =
+      static_cast<WebGPUAllocation*>(output.storage().data_ptr().get());
+  TORCH_CHECK(input_allocation && output_allocation);
+
+  auto& context = torch_webgpu::core::getWebGPUContext();
+  wgpu::BufferDescriptor uniform_descriptor{};
+  uniform_descriptor.label = "torch-webgpu softmax params";
+  uniform_descriptor.usage =
+      wgpu::BufferUsage::Uniform | wgpu::BufferUsage::CopyDst;
+  uniform_descriptor.size = sizeof(Params);
+  auto params_buffer =
+      context.getDevice().CreateBuffer(&uniform_descriptor);
+  context.getQueue().WriteBuffer(params_buffer, 0, &params, sizeof(params));
+
+  auto& kernel = softmax_kernel();
+  wgpu::BindGroupEntry entries[3]{};
+  entries[0].binding = 0;
+  entries[0].buffer = input_allocation->buffer;
+  entries[0].size = input_allocation->buffer.GetSize();
+  entries[1].binding = 1;
+  entries[1].buffer = output_allocation->buffer;
+  entries[1].size = output_allocation->buffer.GetSize();
+  entries[2].binding = 2;
+  entries[2].buffer = params_buffer;
+  entries[2].size = sizeof(Params);
+  wgpu::BindGroupDescriptor bind_group_descriptor{};
+  bind_group_descriptor.layout = kernel.bind_group_layout;
+  bind_group_descriptor.entryCount = 3;
+  bind_group_descriptor.entries = entries;
+  auto bind_group =
+      context.getDevice().CreateBindGroup(&bind_group_descriptor);
+
+  auto encoder = context.getDevice().CreateCommandEncoder();
+  auto pass = encoder.BeginComputePass();
+  pass.SetPipeline(kernel.pipeline);
+  pass.SetBindGroup(0, bind_group);
+  pass.DispatchWorkgroups((params.batch_size + 63) / 64, 1, 1);
+  pass.End();
+  auto command_buffer = encoder.Finish();
+  context.getQueue().Submit(1, &command_buffer);
+  return output;
+}
+
+at::Tensor& softmax_out(
+    const at::Tensor& input,
+    std::int64_t dim,
+    std::optional<at::ScalarType> dtype,
+    at::Tensor& output) {
+  auto result = browser_softmax(input, dim, dtype);
+  output.copy_(result);
+  return output;
+}
+
+at::Tensor log_softmax(
+    const at::Tensor& input,
+    std::int64_t dim,
+    std::optional<at::ScalarType> dtype) {
+  return at::log(browser_softmax(input, dim, dtype));
+}
+
+} // namespace
+
+TORCH_LIBRARY_IMPL(aten, PrivateUse1, module) {
+  module.impl("add.Tensor", TORCH_FN(add_tensor));
+  module.impl("add.out", TORCH_FN(add_out));
+  module.impl("mul.Tensor", TORCH_FN(mul_tensor));
+  module.impl("mul.out", TORCH_FN(mul_out));
+  module.impl("sub.Tensor", TORCH_FN(sub_tensor));
+  module.impl("sub.out", TORCH_FN(sub_out));
+  module.impl("div.Tensor", TORCH_FN(div_tensor));
+  module.impl("div.out", TORCH_FN(div_out));
+  module.impl("mm", TORCH_FN(mm));
+  module.impl("mm.out", TORCH_FN(mm_out));
+  module.impl("softmax.int", TORCH_FN(browser_softmax));
+  module.impl("softmax.int_out", TORCH_FN(softmax_out));
+  module.impl("log_softmax.int", TORCH_FN(log_softmax));
+}
+
+} // namespace torch_webgpu::browser_ops
diff --git a/torch/csrc/webgpu/compat/core/webgpu_allocator.h b/torch/csrc/webgpu/compat/core/webgpu_allocator.h
new file mode 100644
index 0000000..1e3d5d6
--- /dev/null
+++ b/torch/csrc/webgpu/compat/core/webgpu_allocator.h
@@ -0,0 +1,21 @@
+#pragma once
+
+#include <webgpu/webgpu_cpp.h>
+
+#include <cstddef>
+#include <utility>
+
+namespace torch_webgpu::core {
+
+// This deliberately matches the allocation object expected by torch-webgpu's
+// existing operator implementations. The wgpu::Buffer is an RAII wrapper over
+// an ID whose browser GPUBuffer is owned by the side-module EM_JS bridge.
+struct WebGPUAllocation {
+  wgpu::Buffer buffer;
+  std::size_t nbytes;
+
+  WebGPUAllocation(wgpu::Buffer&& value, std::size_t size)
+      : buffer(std::move(value)), nbytes(size) {}
+};
+
+} // namespace torch_webgpu::core
diff --git a/torch/csrc/webgpu/compat/core/webgpu_context.h b/torch/csrc/webgpu/compat/core/webgpu_context.h
new file mode 100644
index 0000000..f3c1dc8
--- /dev/null
+++ b/torch/csrc/webgpu/compat/core/webgpu_context.h
@@ -0,0 +1,21 @@
+#pragma once
+
+#include <webgpu/webgpu_cpp.h>
+
+namespace torch_webgpu::core {
+
+class WebGPUContext {
+ public:
+  void initialize();
+  bool is_initialized() const;
+  wgpu::Device getDevice() const;
+  wgpu::Queue getQueue() const;
+
+ private:
+  wgpu::Device device_;
+  wgpu::Queue queue_;
+};
+
+WebGPUContext& getWebGPUContext();
+
+} // namespace torch_webgpu::core
diff --git a/torch/csrc/webgpu/emdawnwebgpu_bridge.cpp b/torch/csrc/webgpu/emdawnwebgpu_bridge.cpp
new file mode 100644
index 0000000..1d1a104
--- /dev/null
+++ b/torch/csrc/webgpu/emdawnwebgpu_bridge.cpp
@@ -0,0 +1,871 @@
+// Side-module compute profile for Emdawnwebgpu's Dawn-style C/C++ headers.
+//
+// Emdawnwebgpu's full library_webgpu*.js implementation is a final-link
+// JavaScript library and therefore cannot be installed by a Pyodide side
+// module. Emscripten's dynamic loader does, however, reconstruct EM_JS bodies
+// exported by a side module. This file implements only the WebGPU C entry
+// points used by the imported torch-webgpu eager kernels and stores browser
+// objects behind opaque 32-bit handles.
+
+#include <emscripten.h>
+#include <webgpu/webgpu.h>
+
+#include <cstddef>
+#include <cstdint>
+#include <cstring>
+#include <limits>
+#include <vector>
+
+namespace {
+
+struct LayoutEntry {
+  std::uint32_t binding;
+  std::uint32_t visibility;
+  std::uint32_t buffer_type;
+  std::uint32_t min_binding_size;
+};
+
+struct BindEntry {
+  std::uint32_t binding;
+  std::uint32_t buffer;
+  std::uint32_t offset;
+  std::uint32_t size;
+};
+
+static_assert(sizeof(LayoutEntry) == 16);
+static_assert(sizeof(BindEntry) == 16);
+
+template <typename Handle>
+std::uint32_t handle_id(Handle handle) {
+  return static_cast<std::uint32_t>(
+      reinterpret_cast<std::uintptr_t>(handle));
+}
+
+template <typename Handle>
+Handle make_handle(std::uint32_t id) {
+  return reinterpret_cast<Handle>(static_cast<std::uintptr_t>(id));
+}
+
+std::uint32_t checked_u32(std::uint64_t value) {
+  if (value > std::numeric_limits<std::uint32_t>::max()) {
+    return 0;
+  }
+  return static_cast<std::uint32_t>(value);
+}
+
+std::size_t string_length(WGPUStringView value) {
+  if (value.data == nullptr) {
+    return 0;
+  }
+  return value.length == WGPU_STRLEN ? std::strlen(value.data) : value.length;
+}
+
+EM_JS(void, twgpu_add_ref, (std::uint32_t id), {
+  const state = globalThis.__torchWebGPU;
+  const record = state && state.objects && state.objects.get(id >>> 0);
+  if (record) {
+    record.refs++;
+    record.pendingRelease = false;
+  }
+});
+
+EM_JS(void, twgpu_release, (std::uint32_t id), {
+  const state = globalThis.__torchWebGPU;
+  const record = state && state.objects && state.objects.get(id >>> 0);
+  if (!record) return;
+  if (record.imported) {
+    // JavaScript owns the base reference for the imported device and queue.
+    // Balance temporary webgpu_cpp RAII copies without deleting that owner.
+    if (record.refs > 1) record.refs--;
+    return;
+  }
+  record.refs--;
+  if (record.refs > 0) return;
+  if (record.type === "buffer" &&
+      (state.batchDepth > 0 || state.pendingCommandBuffers.length !== 0)) {
+    // A finished command buffer keeps the GPUBuffer object alive, but an
+    // explicit GPUBuffer.destroy() before queue.submit() invalidates it.
+    // Tensor temporaries can reach refcount zero during an eager forward, so
+    // postpone destruction until the pending command buffers are submitted.
+    record.pendingRelease = true;
+    state.deferredBufferReleases.add(id >>> 0);
+    return;
+  }
+  state.objects.delete(id >>> 0);
+  if (record.type === "buffer") {
+    state.buffers.delete(id >>> 0);
+    if (record.value && !record.destroyed) record.value.destroy();
+    state.bufferReleases++;
+  }
+  state.resourceReleases++;
+});
+
+EM_JS(void, twgpu_batch_begin, (), {
+  const state = globalThis.__torchWebGPU;
+  if (state) state.batchDepth++;
+});
+
+EM_JS(int, twgpu_batch_end, (), {
+  const state = globalThis.__torchWebGPU;
+  if (!state || state.batchDepth <= 0) return -1;
+  state.batchDepth--;
+  return state.batchDepth === 0 ? 1 : 0;
+});
+
+EM_JS(void, twgpu_flush_pending, (), {
+  const state = globalThis.__torchWebGPU;
+  if (!state) return;
+  if (state.pendingCommandBuffers.length !== 0) {
+    // Logical eager command buffers recorded inside a batch all share one
+    // browser command encoder. Finish it only at a required ordering boundary
+    // or at the outer batch exit. Keep both the finished command buffer and
+    // the logical pending list intact if submit throws so the resources stay
+    // live and a validation failure cannot silently discard work.
+    if (!state.batchCommandBuffer) {
+      if (!state.batchCommandEncoder) {
+        throw new Error("pending WebGPU batch has no command encoder");
+      }
+      state.batchCommandBuffer = state.batchCommandEncoder.finish();
+      state.batchCommandEncoder = null;
+      state.commandBufferCreations++;
+    }
+    state.queue.submit([state.batchCommandBuffer]);
+    state.commandSubmissions++;
+    state.commandBuffersSubmitted += state.pendingCommandBuffers.length;
+    state.physicalCommandBuffersSubmitted++;
+    state.batchFlushes++;
+    state.pendingCommandBuffers.length = 0;
+    state.pendingBufferUses.clear();
+    state.batchCommandBuffer = null;
+  } else if (state.batchDepth === 0) {
+    // Drop an unsubmitted encoder left by an operation that failed before its
+    // logical queue submission. No accepted eager work can be present here.
+    state.batchCommandEncoder = null;
+    state.batchCommandBuffer = null;
+  }
+  for (const id of state.deferredBufferReleases) {
+    const record = state.objects.get(id);
+    if (!record || record.type !== "buffer") continue;
+    if (record.pendingDestroy && record.value && !record.destroyed) {
+      record.value.destroy();
+      record.destroyed = true;
+    }
+    if (record.pendingRelease && record.refs <= 0) {
+      state.objects.delete(id);
+      state.buffers.delete(id);
+      if (record.value && !record.destroyed) record.value.destroy();
+      state.bufferReleases++;
+      state.resourceReleases++;
+    }
+    record.pendingDestroy = false;
+    record.pendingRelease = false;
+  }
+  state.deferredBufferReleases.clear();
+});
+
+EM_JS(int, twgpu_pending_uses_buffer, (std::uint32_t id), {
+  const state = globalThis.__torchWebGPU;
+  return state && state.pendingBufferUses.has(id >>> 0) ? 1 : 0;
+});
+
+EM_JS(
+    std::uint32_t,
+    twgpu_create_buffer,
+    (std::uint32_t size,
+     std::uint32_t usage,
+     int mapped_at_creation),
+    {
+      const state = globalThis.__torchWebGPU;
+      if (!state || !state.device || !state.objects || !state.buffers) return 0;
+      const id = state.nextObjectId++ >>> 0;
+      const buffer = state.device.createBuffer({
+        label: "PyTorch WebGPU buffer",
+        size: Math.max(4, size >>> 0),
+        usage: usage >>> 0,
+        mappedAtCreation: !!mapped_at_creation,
+      });
+      state.objects.set(id, {
+        value: buffer,
+        type: "buffer",
+        refs: 1,
+        size: Math.max(4, size >>> 0),
+        imported: false,
+        destroyed: false,
+        pendingDestroy: false,
+        pendingRelease: false,
+      });
+      state.buffers.set(id, buffer);
+      state.bufferAllocations++;
+      return id;
+    });
+
+EM_JS(void, twgpu_destroy_buffer, (std::uint32_t id), {
+  const state = globalThis.__torchWebGPU;
+  const record = state && state.objects && state.objects.get(id >>> 0);
+  if (record && record.type === "buffer" && record.value) {
+    if (state.batchDepth > 0 || state.pendingCommandBuffers.length !== 0) {
+      record.pendingDestroy = true;
+      state.deferredBufferReleases.add(id >>> 0);
+    } else if (!record.destroyed) {
+      record.value.destroy();
+      record.destroyed = true;
+    }
+  }
+});
+
+EM_JS(double, twgpu_buffer_size, (std::uint32_t id), {
+  const state = globalThis.__torchWebGPU;
+  const record = state && state.objects && state.objects.get(id >>> 0);
+  return record && record.type === "buffer" ? record.size : 0;
+});
+
+EM_JS(
+    std::uint32_t,
+    twgpu_create_shader_module,
+    (const char* code, std::uint32_t length),
+    {
+      const state = globalThis.__torchWebGPU;
+      const shader = state.device.createShaderModule({
+        label: "torch-webgpu WGSL",
+        code: UTF8ToString(code >>> 0, length >>> 0),
+      });
+      const id = state.nextObjectId++ >>> 0;
+      state.objects.set(id, {
+        value: shader, type: "shader", refs: 1, imported: false,
+      });
+      state.shaderCompilations++;
+      return id;
+    });
+
+EM_JS(
+    std::uint32_t,
+    twgpu_create_bind_group_layout,
+    (const LayoutEntry* entries, std::uint32_t count),
+    {
+      const state = globalThis.__torchWebGPU;
+      const result = [];
+      const base = entries >>> 2;
+      for (let i = 0; i < (count >>> 0); i++) {
+        const offset = base + i * 4;
+        const typeCode = HEAPU32[offset + 2];
+        const type = typeCode === 2 ? "uniform" :
+          typeCode === 3 ? "storage" :
+          typeCode === 4 ? "read-only-storage" : undefined;
+        if (!type) throw new Error(`unsupported WGPUBufferBindingType ${typeCode}`);
+        result.push({
+          binding: HEAPU32[offset],
+          visibility: HEAPU32[offset + 1],
+          buffer: {
+            type,
+            hasDynamicOffset: false,
+            minBindingSize: HEAPU32[offset + 3],
+          },
+        });
+      }
+      const value = state.device.createBindGroupLayout({entries: result});
+      const id = state.nextObjectId++ >>> 0;
+      state.objects.set(id, {
+        value, type: "bind-group-layout", refs: 1, imported: false,
+      });
+      return id;
+    });
+
+EM_JS(
+    std::uint32_t,
+    twgpu_create_pipeline_layout,
+    (const std::uint32_t* layouts, std::uint32_t count),
+    {
+      const state = globalThis.__torchWebGPU;
+      const values = [];
+      const base = layouts >>> 2;
+      for (let i = 0; i < (count >>> 0); i++) {
+        const record = state.objects.get(HEAPU32[base + i]);
+        if (!record) throw new Error("released bind group layout");
+        values.push(record.value);
+      }
+      const value = state.device.createPipelineLayout({bindGroupLayouts: values});
+      const id = state.nextObjectId++ >>> 0;
+      state.objects.set(id, {
+        value, type: "pipeline-layout", refs: 1, imported: false,
+      });
+      return id;
+    });
+
+EM_JS(
+    std::uint32_t,
+    twgpu_create_compute_pipeline,
+    (std::uint32_t layout_id,
+     std::uint32_t shader_id,
+     const char* entry_point,
+     std::uint32_t entry_point_length),
+    {
+      const state = globalThis.__torchWebGPU;
+      const layout = state.objects.get(layout_id >>> 0);
+      const shader = state.objects.get(shader_id >>> 0);
+      if (!layout || !shader) throw new Error("released pipeline dependency");
+      const value = state.device.createComputePipeline({
+        layout: layout.value,
+        compute: {
+          "module": shader.value,
+          entryPoint: UTF8ToString(entry_point >>> 0, entry_point_length >>> 0),
+        },
+      });
+      const id = state.nextObjectId++ >>> 0;
+      state.objects.set(id, {
+        value, type: "compute-pipeline", refs: 1, imported: false,
+      });
+      state.pipelineCreations++;
+      return id;
+    });
+
+EM_JS(
+    std::uint32_t,
+    twgpu_create_bind_group,
+    (std::uint32_t layout_id,
+     const BindEntry* entries,
+     std::uint32_t count),
+    {
+      const state = globalThis.__torchWebGPU;
+      const layout = state.objects.get(layout_id >>> 0);
+      if (!layout) throw new Error("released bind group layout");
+      const result = [];
+      const bufferIds = [];
+      const base = entries >>> 2;
+      for (let i = 0; i < (count >>> 0); i++) {
+        const offset = base + i * 4;
+        const bufferId = HEAPU32[offset + 1];
+        const buffer = state.objects.get(bufferId);
+        if (!buffer) throw new Error("released bind group buffer");
+        bufferIds.push(bufferId);
+        result.push({
+          binding: HEAPU32[offset],
+          resource: {
+            buffer: buffer.value,
+            offset: HEAPU32[offset + 2],
+            size: HEAPU32[offset + 3],
+          },
+        });
+      }
+      const value = state.device.createBindGroup({
+        layout: layout.value, entries: result,
+      });
+      const id = state.nextObjectId++ >>> 0;
+      state.objects.set(id, {
+        value,
+        type: "bind-group",
+        refs: 1,
+        imported: false,
+        bufferIds,
+      });
+      return id;
+    });
+
+EM_JS(std::uint32_t, twgpu_create_command_encoder, (), {
+  const state = globalThis.__torchWebGPU;
+  const batched = state.batchDepth > 0;
+  let value;
+  if (batched) {
+    if (state.batchCommandBuffer) {
+      throw new Error("failed WebGPU batch submission must be flushed first");
+    }
+    if (!state.batchCommandEncoder) {
+      state.batchCommandEncoder = state.device.createCommandEncoder();
+      state.commandEncoderCreations++;
+    }
+    value = state.batchCommandEncoder;
+  } else {
+    value = state.device.createCommandEncoder();
+    state.commandEncoderCreations++;
+  }
+  const id = state.nextObjectId++ >>> 0;
+  state.objects.set(id, {
+    value,
+    type: "command-encoder",
+    refs: 1,
+    imported: false,
+    batched,
+    bufferIds: new Set(),
+  });
+  return id;
+});
+
+EM_JS(
+    std::uint32_t,
+    twgpu_begin_compute_pass,
+    (std::uint32_t encoder_id),
+    {
+      const state = globalThis.__torchWebGPU;
+      const encoder = state.objects.get(encoder_id >>> 0);
+      if (!encoder) throw new Error("released command encoder");
+      const value = encoder.value.beginComputePass();
+      const id = state.nextObjectId++ >>> 0;
+      state.objects.set(id, {
+        value,
+        type: "compute-pass",
+        refs: 1,
+        imported: false,
+        encoderId: encoder_id >>> 0,
+      });
+      return id;
+    });
+
+EM_JS(
+    void,
+    twgpu_compute_pass_set_pipeline,
+    (std::uint32_t pass_id, std::uint32_t pipeline_id),
+    {
+      const state = globalThis.__torchWebGPU;
+      state.objects.get(pass_id >>> 0).value.setPipeline(
+        state.objects.get(pipeline_id >>> 0).value);
+    });
+
+EM_JS(
+    void,
+    twgpu_compute_pass_set_bind_group,
+    (std::uint32_t pass_id,
+     std::uint32_t group_index,
+     std::uint32_t bind_group_id),
+    {
+      const state = globalThis.__torchWebGPU;
+      const pass = state.objects.get(pass_id >>> 0);
+      const bindGroup = state.objects.get(bind_group_id >>> 0);
+      if (!pass || !bindGroup) throw new Error("released bind group dependency");
+      pass.value.setBindGroup(group_index >>> 0, bindGroup.value);
+      const encoder = state.objects.get(pass.encoderId);
+      if (!encoder) throw new Error("released command encoder");
+      for (const bufferId of bindGroup.bufferIds) {
+        encoder.bufferIds.add(bufferId);
+      }
+    });
+
+EM_JS(
+    void,
+    twgpu_compute_pass_dispatch,
+    (std::uint32_t pass_id,
+     std::uint32_t x,
+     std::uint32_t y,
+     std::uint32_t z),
+    {
+      const state = globalThis.__torchWebGPU;
+      state.objects.get(pass_id >>> 0).value.dispatchWorkgroups(
+        x >>> 0, y >>> 0, z >>> 0);
+      state.dispatches++;
+    });
+
+EM_JS(void, twgpu_compute_pass_end, (std::uint32_t pass_id), {
+  const state = globalThis.__torchWebGPU;
+  state.objects.get(pass_id >>> 0).value.end();
+});
+
+EM_JS(
+    void,
+    twgpu_encoder_copy_buffer,
+    (std::uint32_t encoder_id,
+     std::uint32_t source_id,
+     std::uint32_t source_offset,
+     std::uint32_t destination_id,
+     std::uint32_t destination_offset,
+     std::uint32_t size),
+    {
+      const state = globalThis.__torchWebGPU;
+      const encoder = state.objects.get(encoder_id >>> 0);
+      encoder.value.copyBufferToBuffer(
+        state.objects.get(source_id >>> 0).value,
+        source_offset >>> 0,
+        state.objects.get(destination_id >>> 0).value,
+        destination_offset >>> 0,
+        size >>> 0);
+      encoder.bufferIds.add(source_id >>> 0);
+      encoder.bufferIds.add(destination_id >>> 0);
+      state.gpuCopies++;
+    });
+
+EM_JS(
+    std::uint32_t,
+    twgpu_finish_command_encoder,
+    (std::uint32_t encoder_id),
+    {
+      const state = globalThis.__torchWebGPU;
+      const encoder = state.objects.get(encoder_id >>> 0);
+      if (!encoder) throw new Error("released command encoder");
+      const value = encoder.batched ? null : encoder.value.finish();
+      if (!encoder.batched) state.commandBufferCreations++;
+      const id = state.nextObjectId++ >>> 0;
+      state.objects.set(id, {
+        value,
+        type: "command-buffer",
+        refs: 1,
+        imported: false,
+        batched: encoder.batched,
+        bufferIds: Array.from(encoder.bufferIds),
+      });
+      return id;
+    });
+
+EM_JS(
+    void,
+    twgpu_queue_submit,
+    (const std::uint32_t* commands, std::uint32_t count),
+    {
+      const state = globalThis.__torchWebGPU;
+      const records = [];
+      const bufferIds = new Set();
+      const base = commands >>> 2;
+      for (let i = 0; i < (count >>> 0); i++) {
+        const record = state.objects.get(HEAPU32[base + i]);
+        if (!record) throw new Error("released command buffer");
+        records.push(record);
+        for (const bufferId of record.bufferIds) bufferIds.add(bufferId);
+      }
+      if (state.batchDepth > 0) {
+        for (const record of records) {
+          if (!record.batched) {
+            throw new Error("non-batched command buffer submitted in a batch");
+          }
+          // The array length intentionally counts logical eager command
+          // buffers for compatibility and diagnostics; their commands live
+          // in batchCommandEncoder until the next physical flush.
+          state.pendingCommandBuffers.push(null);
+        }
+        for (const bufferId of bufferIds) state.pendingBufferUses.add(bufferId);
+        state.batchedCommandBuffers += records.length;
+        return;
+      }
+      const result = [];
+      for (const record of records) {
+        if (record.batched) {
+          throw new Error("batched command buffer submitted outside its scope");
+        }
+        result.push(record.value);
+      }
+      state.queue.submit(result);
+      state.commandSubmissions++;
+      state.commandBuffersSubmitted += result.length;
+      state.physicalCommandBuffersSubmitted += result.length;
+    });
+
+EM_JS(
+    void,
+    twgpu_queue_write_buffer,
+    (std::uint32_t buffer_id,
+     std::uint32_t buffer_offset,
+     const void* data,
+     std::uint32_t size),
+    {
+      const state = globalThis.__torchWebGPU;
+      const buffer = state.objects.get(buffer_id >>> 0);
+      if (!buffer) throw new Error("released destination buffer");
+      if (state.pendingBufferUses.has(buffer_id >>> 0)) {
+        throw new Error("pending WebGPU commands must be flushed before writeBuffer");
+      }
+      // HEAPU8 is read synchronously. Do not retain this view across an await;
+      // Pyodide uses growable Wasm memory and refreshes its global heap views.
+      state.queue.writeBuffer(
+        buffer.value,
+        buffer_offset >>> 0,
+        HEAPU8,
+        data >>> 0,
+        size >>> 0);
+      state.uploads++;
+    });
+
+} // namespace
+
+extern "C" {
+
+void torch_webgpu_batch_begin() {
+  twgpu_batch_begin();
+}
+
+int torch_webgpu_batch_end() {
+  const int result = twgpu_batch_end();
+  if (result > 0) {
+    twgpu_flush_pending();
+  }
+  return result;
+}
+
+void torch_webgpu_flush() {
+  twgpu_flush_pending();
+}
+
+WGPUBuffer wgpuDeviceCreateBuffer(
+    WGPUDevice,
+    const WGPUBufferDescriptor* descriptor) {
+  if (descriptor == nullptr || descriptor->mappedAtCreation ||
+      descriptor->nextInChain != nullptr) {
+    return nullptr;
+  }
+  const auto size = checked_u32(descriptor->size);
+  const auto usage = checked_u32(descriptor->usage);
+  if (size == 0 && descriptor->size != 0) {
+    return nullptr;
+  }
+  return make_handle<WGPUBuffer>(twgpu_create_buffer(size, usage, 0));
+}
+
+void wgpuBufferDestroy(WGPUBuffer buffer) {
+  twgpu_destroy_buffer(handle_id(buffer));
+}
+
+std::uint64_t wgpuBufferGetSize(WGPUBuffer buffer) {
+  return static_cast<std::uint64_t>(twgpu_buffer_size(handle_id(buffer)));
+}
+
+WGPUShaderModule wgpuDeviceCreateShaderModule(
+    WGPUDevice,
+    const WGPUShaderModuleDescriptor* descriptor) {
+  if (descriptor == nullptr || descriptor->nextInChain == nullptr ||
+      descriptor->nextInChain->sType != WGPUSType_ShaderSourceWGSL) {
+    return nullptr;
+  }
+  const auto* source = reinterpret_cast<const WGPUShaderSourceWGSL*>(
+      descriptor->nextInChain);
+  const auto length = string_length(source->code);
+  if (source->code.data == nullptr ||
+      length > std::numeric_limits<std::uint32_t>::max()) {
+    return nullptr;
+  }
+  return make_handle<WGPUShaderModule>(twgpu_create_shader_module(
+      source->code.data, static_cast<std::uint32_t>(length)));
+}
+
+WGPUBindGroupLayout wgpuDeviceCreateBindGroupLayout(
+    WGPUDevice,
+    const WGPUBindGroupLayoutDescriptor* descriptor) {
+  if (descriptor == nullptr || descriptor->nextInChain != nullptr ||
+      descriptor->entryCount > std::numeric_limits<std::uint32_t>::max()) {
+    return nullptr;
+  }
+  std::vector<LayoutEntry> entries;
+  entries.reserve(descriptor->entryCount);
+  for (std::size_t index = 0; index < descriptor->entryCount; ++index) {
+    const auto& entry = descriptor->entries[index];
+    if (entry.nextInChain != nullptr || entry.bindingArraySize != 0 ||
+        entry.buffer.nextInChain != nullptr ||
+        entry.buffer.hasDynamicOffset) {
+      return nullptr;
+    }
+    const auto min_size = checked_u32(entry.buffer.minBindingSize);
+    if (min_size == 0 && entry.buffer.minBindingSize != 0) {
+      return nullptr;
+    }
+    entries.push_back({
+        entry.binding,
+        checked_u32(entry.visibility),
+        static_cast<std::uint32_t>(entry.buffer.type),
+        min_size});
+  }
+  return make_handle<WGPUBindGroupLayout>(twgpu_create_bind_group_layout(
+      entries.data(), static_cast<std::uint32_t>(entries.size())));
+}
+
+WGPUPipelineLayout wgpuDeviceCreatePipelineLayout(
+    WGPUDevice,
+    const WGPUPipelineLayoutDescriptor* descriptor) {
+  if (descriptor == nullptr || descriptor->nextInChain != nullptr ||
+      descriptor->bindGroupLayoutCount >
+          std::numeric_limits<std::uint32_t>::max()) {
+    return nullptr;
+  }
+  std::vector<std::uint32_t> layouts;
+  layouts.reserve(descriptor->bindGroupLayoutCount);
+  for (std::size_t index = 0; index < descriptor->bindGroupLayoutCount;
+       ++index) {
+    layouts.push_back(handle_id(descriptor->bindGroupLayouts[index]));
+  }
+  return make_handle<WGPUPipelineLayout>(twgpu_create_pipeline_layout(
+      layouts.data(), static_cast<std::uint32_t>(layouts.size())));
+}
+
+WGPUComputePipeline wgpuDeviceCreateComputePipeline(
+    WGPUDevice,
+    const WGPUComputePipelineDescriptor* descriptor) {
+  if (descriptor == nullptr || descriptor->nextInChain != nullptr ||
+      descriptor->compute.nextInChain != nullptr ||
+      descriptor->compute.constantCount != 0 ||
+      descriptor->layout == nullptr || descriptor->compute.module == nullptr) {
+    return nullptr;
+  }
+  const auto length = string_length(descriptor->compute.entryPoint);
+  if (descriptor->compute.entryPoint.data == nullptr ||
+      length > std::numeric_limits<std::uint32_t>::max()) {
+    return nullptr;
+  }
+  return make_handle<WGPUComputePipeline>(twgpu_create_compute_pipeline(
+      handle_id(descriptor->layout),
+      handle_id(descriptor->compute.module),
+      descriptor->compute.entryPoint.data,
+      static_cast<std::uint32_t>(length)));
+}
+
+WGPUBindGroup wgpuDeviceCreateBindGroup(
+    WGPUDevice,
+    const WGPUBindGroupDescriptor* descriptor) {
+  if (descriptor == nullptr || descriptor->nextInChain != nullptr ||
+      descriptor->layout == nullptr ||
+      descriptor->entryCount > std::numeric_limits<std::uint32_t>::max()) {
+    return nullptr;
+  }
+  std::vector<BindEntry> entries;
+  entries.reserve(descriptor->entryCount);
+  for (std::size_t index = 0; index < descriptor->entryCount; ++index) {
+    const auto& entry = descriptor->entries[index];
+    const auto offset = checked_u32(entry.offset);
+    const auto size = checked_u32(entry.size);
+    if (entry.nextInChain != nullptr || entry.buffer == nullptr ||
+        entry.sampler != nullptr || entry.textureView != nullptr ||
+        (offset == 0 && entry.offset != 0) ||
+        (size == 0 && entry.size != 0)) {
+      return nullptr;
+    }
+    entries.push_back({entry.binding, handle_id(entry.buffer), offset, size});
+  }
+  return make_handle<WGPUBindGroup>(twgpu_create_bind_group(
+      handle_id(descriptor->layout),
+      entries.data(),
+      static_cast<std::uint32_t>(entries.size())));
+}
+
+WGPUCommandEncoder wgpuDeviceCreateCommandEncoder(
+    WGPUDevice,
+    const WGPUCommandEncoderDescriptor* descriptor) {
+  if (descriptor != nullptr && descriptor->nextInChain != nullptr) {
+    return nullptr;
+  }
+  return make_handle<WGPUCommandEncoder>(twgpu_create_command_encoder());
+}
+
+WGPUComputePassEncoder wgpuCommandEncoderBeginComputePass(
+    WGPUCommandEncoder encoder,
+    const WGPUComputePassDescriptor* descriptor) {
+  if (descriptor != nullptr &&
+      (descriptor->nextInChain != nullptr ||
+       descriptor->timestampWrites != nullptr)) {
+    return nullptr;
+  }
+  return make_handle<WGPUComputePassEncoder>(
+      twgpu_begin_compute_pass(handle_id(encoder)));
+}
+
+void wgpuComputePassEncoderSetPipeline(
+    WGPUComputePassEncoder pass,
+    WGPUComputePipeline pipeline) {
+  twgpu_compute_pass_set_pipeline(handle_id(pass), handle_id(pipeline));
+}
+
+void wgpuComputePassEncoderSetBindGroup(
+    WGPUComputePassEncoder pass,
+    std::uint32_t group_index,
+    WGPUBindGroup bind_group,
+    std::size_t dynamic_offset_count,
+    const std::uint32_t*) {
+  if (dynamic_offset_count != 0) {
+    return;
+  }
+  twgpu_compute_pass_set_bind_group(
+      handle_id(pass), group_index, handle_id(bind_group));
+}
+
+void wgpuComputePassEncoderDispatchWorkgroups(
+    WGPUComputePassEncoder pass,
+    std::uint32_t x,
+    std::uint32_t y,
+    std::uint32_t z) {
+  twgpu_compute_pass_dispatch(handle_id(pass), x, y, z);
+}
+
+void wgpuComputePassEncoderEnd(WGPUComputePassEncoder pass) {
+  twgpu_compute_pass_end(handle_id(pass));
+}
+
+void wgpuCommandEncoderCopyBufferToBuffer(
+    WGPUCommandEncoder encoder,
+    WGPUBuffer source,
+    std::uint64_t source_offset,
+    WGPUBuffer destination,
+    std::uint64_t destination_offset,
+    std::uint64_t size) {
+  const auto source_offset_u32 = checked_u32(source_offset);
+  const auto destination_offset_u32 = checked_u32(destination_offset);
+  const auto size_u32 = checked_u32(size);
+  if ((source_offset_u32 == 0 && source_offset != 0) ||
+      (destination_offset_u32 == 0 && destination_offset != 0) ||
+      (size_u32 == 0 && size != 0)) {
+    return;
+  }
+  twgpu_encoder_copy_buffer(
+      handle_id(encoder),
+      handle_id(source),
+      source_offset_u32,
+      handle_id(destination),
+      destination_offset_u32,
+      size_u32);
+}
+
+WGPUCommandBuffer wgpuCommandEncoderFinish(
+    WGPUCommandEncoder encoder,
+    const WGPUCommandBufferDescriptor* descriptor) {
+  if (descriptor != nullptr && descriptor->nextInChain != nullptr) {
+    return nullptr;
+  }
+  return make_handle<WGPUCommandBuffer>(
+      twgpu_finish_command_encoder(handle_id(encoder)));
+}
+
+void wgpuQueueSubmit(
+    WGPUQueue,
+    std::size_t command_count,
+    const WGPUCommandBuffer* commands) {
+  if (command_count > std::numeric_limits<std::uint32_t>::max()) {
+    return;
+  }
+  std::vector<std::uint32_t> ids;
+  ids.reserve(command_count);
+  for (std::size_t index = 0; index < command_count; ++index) {
+    ids.push_back(handle_id(commands[index]));
+  }
+  twgpu_queue_submit(ids.data(), static_cast<std::uint32_t>(ids.size()));
+}
+
+void wgpuQueueWriteBuffer(
+    WGPUQueue,
+    WGPUBuffer buffer,
+    std::uint64_t buffer_offset,
+    const void* data,
+    std::size_t size) {
+  const auto offset_u32 = checked_u32(buffer_offset);
+  const auto size_u32 = checked_u32(size);
+  if ((offset_u32 == 0 && buffer_offset != 0) ||
+      (size_u32 == 0 && size != 0)) {
+    return;
+  }
+  if (twgpu_pending_uses_buffer(handle_id(buffer))) {
+    // queue.writeBuffer is ordered at call time. Submit earlier work that
+    // references this destination first; writes to independent buffers (such
+    // as per-dispatch uniforms) do not need to split an explicit batch.
+    twgpu_flush_pending();
+  }
+  twgpu_queue_write_buffer(
+      handle_id(buffer), offset_u32, data, size_u32);
+}
+
+#define TWGPU_REFCOUNT_FUNCTIONS(Name, CType)                         \
+  void wgpu##Name##AddRef(CType handle) { twgpu_add_ref(handle_id(handle)); } \
+  void wgpu##Name##Release(CType handle) { twgpu_release(handle_id(handle)); }
+
+TWGPU_REFCOUNT_FUNCTIONS(BindGroup, WGPUBindGroup)
+TWGPU_REFCOUNT_FUNCTIONS(BindGroupLayout, WGPUBindGroupLayout)
+TWGPU_REFCOUNT_FUNCTIONS(Buffer, WGPUBuffer)
+TWGPU_REFCOUNT_FUNCTIONS(CommandBuffer, WGPUCommandBuffer)
+TWGPU_REFCOUNT_FUNCTIONS(CommandEncoder, WGPUCommandEncoder)
+TWGPU_REFCOUNT_FUNCTIONS(ComputePassEncoder, WGPUComputePassEncoder)
+TWGPU_REFCOUNT_FUNCTIONS(ComputePipeline, WGPUComputePipeline)
+TWGPU_REFCOUNT_FUNCTIONS(Device, WGPUDevice)
+TWGPU_REFCOUNT_FUNCTIONS(PipelineLayout, WGPUPipelineLayout)
+TWGPU_REFCOUNT_FUNCTIONS(Queue, WGPUQueue)
+TWGPU_REFCOUNT_FUNCTIONS(ShaderModule, WGPUShaderModule)
+
+#undef TWGPU_REFCOUNT_FUNCTIONS
+
+} // extern "C"
diff --git a/torch/csrc/webgpu/webgpu_backend.cpp b/torch/csrc/webgpu/webgpu_backend.cpp
new file mode 100644
index 0000000..5488ea5
--- /dev/null
+++ b/torch/csrc/webgpu/webgpu_backend.cpp
@@ -0,0 +1,474 @@
+// Browser storage, device plumbing, copies, and metadata operators for the
+// torch-webgpu eager-kernel port. Compute dispatch itself uses the pinned
+// Emdawnwebgpu Dawn-style headers through emdawnwebgpu_bridge.cpp.
+
+#include <ATen/ATen.h>
+#include <ATen/EmptyTensor.h>
+#include <ATen/core/grad_mode.h>
+#include <ATen/ops/as_strided_cpu_dispatch.h>
+#include <ATen/ops/view_cpu_dispatch.h>
+#include <c10/core/Allocator.h>
+#include <c10/core/Device.h>
+#include <c10/core/Stream.h>
+#include <c10/core/impl/DeviceGuardImplInterface.h>
+#include <c10/util/irange.h>
+#include <torch/library.h>
+#include <webgpu/webgpu_cpp.h>
+
+#include "core/webgpu_allocator.h"
+#include "core/webgpu_context.h"
+
+#include <cstddef>
+#include <cstdint>
+#include <cstring>
+#include <limits>
+#include <optional>
+#include <utility>
+#include <vector>
+
+extern "C" {
+void torch_webgpu_batch_begin();
+int torch_webgpu_batch_end();
+void torch_webgpu_flush();
+}
+
+namespace torch_webgpu::browser {
+namespace {
+
+using core::WebGPUAllocation;
+
+void delete_allocation(void* pointer) {
+  delete static_cast<WebGPUAllocation*>(pointer);
+}
+
+struct WebGPUAllocator final : c10::Allocator {
+  c10::DataPtr allocate(std::size_t nbytes) override {
+    if (nbytes == 0) {
+      return {
+          nullptr,
+          nullptr,
+          &delete_allocation,
+          c10::Device(c10::DeviceType::PrivateUse1, 0)};
+    }
+    TORCH_CHECK(
+        core::getWebGPUContext().is_initialized(),
+        "WebGPU is not initialized; call `torch.webgpu.init()` first");
+    TORCH_CHECK(
+        nbytes <= UINT32_MAX - std::size_t(3),
+        "WebGPU allocation is too large: ",
+        nbytes,
+        " bytes");
+    const auto aligned_nbytes = (nbytes + 3) & ~std::size_t(3);
+    wgpu::BufferDescriptor descriptor{};
+    descriptor.label = "PyTorch WebGPU tensor";
+    descriptor.size = aligned_nbytes;
+    descriptor.usage = wgpu::BufferUsage::CopyDst |
+        wgpu::BufferUsage::CopySrc | wgpu::BufferUsage::Storage;
+    descriptor.mappedAtCreation = false;
+    auto buffer = core::getWebGPUContext().getDevice().CreateBuffer(&descriptor);
+    TORCH_CHECK(buffer != nullptr, "WebGPU buffer allocation failed");
+    auto* allocation =
+        new WebGPUAllocation(std::move(buffer), aligned_nbytes);
+    return {
+        allocation,
+        allocation,
+        &delete_allocation,
+        c10::Device(c10::DeviceType::PrivateUse1, 0)};
+  }
+
+  c10::DeleterFnPtr raw_deleter() const override {
+    return &delete_allocation;
+  }
+
+  void copy_data(
+      void*,
+      const void*,
+      std::size_t) const override {
+    TORCH_CHECK_NOT_IMPLEMENTED(
+        false,
+        "WebGPU allocator copy_data is unavailable; use tensor copy operators");
+  }
+};
+
+WebGPUAllocator& allocator() {
+  static WebGPUAllocator instance;
+  return instance;
+}
+
+WebGPUAllocation& allocation(const at::Tensor& tensor) {
+  TORCH_CHECK(tensor.device().is_privateuseone(), "expected a WebGPU tensor");
+  auto* result =
+      static_cast<WebGPUAllocation*>(tensor.storage().data_ptr().get());
+  TORCH_CHECK(result, "WebGPU tensor has no GPUBuffer storage");
+  return *result;
+}
+
+std::uint32_t checked_u32(std::int64_t value, const char* description) {
+  TORCH_CHECK(
+      value >= 0 &&
+          static_cast<std::uint64_t>(value) <=
+              std::numeric_limits<std::uint32_t>::max(),
+      description,
+      " does not fit in uint32 WebGPU metadata");
+  return static_cast<std::uint32_t>(value);
+}
+
+std::uint32_t checked_nbytes(const at::Tensor& tensor) {
+  return checked_u32(
+      tensor.numel() * tensor.element_size(), "tensor byte count");
+}
+
+void check_supported_tensor(
+    const at::Tensor& tensor,
+    const char* operation,
+    bool require_contiguous = true) {
+  TORCH_CHECK(
+      tensor.device().is_privateuseone(), operation, " expected a WebGPU tensor");
+  TORCH_CHECK(
+      tensor.scalar_type() == at::kFloat || tensor.scalar_type() == at::kInt ||
+          tensor.scalar_type() == at::kLong || tensor.scalar_type() == at::kBool,
+      operation,
+      " supports only torch.float32, torch.int32, signed-int32-valued "
+      "torch.int64, and byte-packed torch.bool");
+  TORCH_CHECK(
+      !require_contiguous || tensor.is_contiguous(),
+      operation,
+      " requires a contiguous tensor");
+  TORCH_CHECK(
+      tensor.storage_offset() >= 0,
+      operation,
+      " does not support negative storage offsets");
+}
+
+void check_long_values(const at::Tensor& tensor, const char* operation) {
+  if (tensor.scalar_type() != at::kLong) {
+    return;
+  }
+  TORCH_CHECK(
+      tensor.device().is_cpu() && tensor.is_contiguous(),
+      operation,
+      " Long validation requires a contiguous CPU tensor");
+  const auto* values = tensor.const_data_ptr<std::int64_t>();
+  for (const auto index : c10::irange(tensor.numel())) {
+    TORCH_CHECK(
+        values[index] >= std::numeric_limits<std::int32_t>::min() &&
+            values[index] <= std::numeric_limits<std::int32_t>::max(),
+        operation,
+        " supports torch.int64 storage only for signed-int32 values; element ",
+        index,
+        " is ",
+        values[index]);
+  }
+}
+
+class WebGPUGuardImpl final : public c10::impl::DeviceGuardImplInterface {
+ public:
+  static constexpr c10::DeviceType static_type =
+      c10::DeviceType::PrivateUse1;
+
+  c10::DeviceType type() const override {
+    return static_type;
+  }
+
+  c10::Device exchangeDevice(c10::Device device) const override {
+    setDevice(device);
+    return c10::Device(static_type, 0);
+  }
+
+  c10::Device getDevice() const override {
+    return c10::Device(static_type, 0);
+  }
+
+  void setDevice(c10::Device device) const override {
+    TORCH_CHECK(
+        device.is_privateuseone() && device.index() <= 0,
+        "WebGPU supports only device 0");
+  }
+
+  void uncheckedSetDevice(c10::Device) const noexcept override {}
+
+  c10::DeviceIndex deviceCount() const noexcept override {
+    return core::getWebGPUContext().is_initialized() ? 1 : 0;
+  }
+
+  c10::Stream getStream(c10::Device device) const noexcept override {
+    return c10::Stream(c10::Stream::DEFAULT, device);
+  }
+
+  c10::Stream exchangeStream(c10::Stream stream) const noexcept override {
+    return c10::Stream(c10::Stream::DEFAULT, stream.device());
+  }
+};
+
+at::Tensor empty_memory_format(
+    c10::IntArrayRef size,
+    std::optional<at::ScalarType> dtype,
+    std::optional<at::Layout> layout,
+    std::optional<at::Device> device,
+    std::optional<bool> pin_memory,
+    std::optional<c10::MemoryFormat> memory_format) {
+  const auto resolved_dtype = c10::dtype_or_default(dtype);
+  TORCH_CHECK(
+      resolved_dtype == at::kFloat || resolved_dtype == at::kInt ||
+          resolved_dtype == at::kLong || resolved_dtype == at::kBool,
+      "WebGPU supports only torch.float32, torch.int32, signed-int32-valued "
+      "torch.int64, and byte-packed torch.bool");
+  TORCH_CHECK(
+      c10::layout_or_default(layout) == c10::Layout::Strided,
+      "WebGPU supports only strided tensors");
+  TORCH_CHECK(
+      !c10::pinned_memory_or_default(pin_memory),
+      "WebGPU tensors cannot be pinned");
+  if (device) {
+    TORCH_CHECK(
+        device->is_privateuseone() && device->index() <= 0,
+        "WebGPU supports only device 0");
+  }
+  constexpr c10::DispatchKeySet key_set(c10::DispatchKey::PrivateUse1);
+  return at::detail::empty_generic(
+      size, &allocator(), key_set, resolved_dtype, memory_format);
+}
+
+at::Tensor empty_strided(
+    c10::IntArrayRef size,
+    c10::IntArrayRef stride,
+    std::optional<at::ScalarType> dtype,
+    std::optional<at::Layout> layout,
+    std::optional<at::Device> device,
+    std::optional<bool> pin_memory) {
+  const auto resolved_dtype = c10::dtype_or_default(dtype);
+  TORCH_CHECK(
+      resolved_dtype == at::kFloat || resolved_dtype == at::kInt ||
+          resolved_dtype == at::kLong || resolved_dtype == at::kBool,
+      "WebGPU supports only torch.float32, torch.int32, signed-int32-valued "
+      "torch.int64, and byte-packed torch.bool");
+  TORCH_CHECK(
+      c10::layout_or_default(layout) == c10::Layout::Strided,
+      "WebGPU supports only strided tensors");
+  TORCH_CHECK(
+      !c10::pinned_memory_or_default(pin_memory),
+      "WebGPU tensors cannot be pinned");
+  if (device) {
+    TORCH_CHECK(
+        device->is_privateuseone() && device->index() <= 0,
+        "WebGPU supports only device 0");
+  }
+  constexpr c10::DispatchKeySet key_set(c10::DispatchKey::PrivateUse1);
+  return at::detail::empty_strided_generic(
+      size, stride, &allocator(), key_set, resolved_dtype);
+}
+
+at::Tensor as_strided(
+    const at::Tensor& tensor,
+    c10::SymIntArrayRef size,
+    c10::SymIntArrayRef stride,
+    std::optional<c10::SymInt> storage_offset) {
+  check_supported_tensor(tensor, "WebGPU as_strided", false);
+  return at::cpu::as_strided_symint(
+      tensor, size, stride, std::move(storage_offset));
+}
+
+at::Tensor view(const at::Tensor& tensor, c10::SymIntArrayRef size) {
+  check_supported_tensor(tensor, "WebGPU view", false);
+  return at::cpu::view_symint(tensor, size);
+}
+
+at::Tensor copy_from(
+    const at::Tensor& source,
+    const at::Tensor& destination,
+    bool /*non_blocking*/) {
+  TORCH_CHECK(source.defined(), "source tensor is not defined");
+  TORCH_CHECK(destination.defined(), "destination tensor is not defined");
+
+  if (source.device().is_cpu() && destination.device().is_privateuseone()) {
+    check_supported_tensor(destination, "CPU to WebGPU copy");
+    TORCH_CHECK(
+        source.scalar_type() == destination.scalar_type(),
+        "CPU to WebGPU copy requires matching dtypes");
+    TORCH_CHECK(
+        source.scalar_type() == at::kFloat || source.scalar_type() == at::kInt ||
+            source.scalar_type() == at::kLong ||
+            source.scalar_type() == at::kBool,
+        "CPU to WebGPU copy supports only torch.float32, torch.int32, "
+        "signed-int32-valued torch.int64, and torch.bool");
+    TORCH_CHECK(source.sizes() == destination.sizes(), "copy shape mismatch");
+    const auto contiguous_source =
+        source.is_contiguous() ? source : source.contiguous();
+    check_long_values(contiguous_source, "CPU to WebGPU copy");
+    const auto nbytes = checked_nbytes(contiguous_source);
+    if (nbytes == 0) {
+      return destination;
+    }
+    auto& destination_allocation = allocation(destination);
+    const auto destination_offset = checked_u32(
+        destination.storage_offset() * destination.element_size(),
+        "destination byte offset");
+    const auto upload_nbytes = source.scalar_type() == at::kBool
+        ? (static_cast<std::uint64_t>(nbytes) + 3u) & ~std::uint64_t(3)
+        : static_cast<std::uint64_t>(nbytes);
+    TORCH_CHECK(
+        source.scalar_type() != at::kBool || destination_offset == 0,
+        "CPU to WebGPU Bool copy requires a destination with storage_offset 0");
+    TORCH_CHECK(
+        static_cast<std::uint64_t>(destination_offset) + upload_nbytes <=
+            destination_allocation.nbytes,
+        "CPU to WebGPU copy exceeds destination storage");
+    const void* upload_data = contiguous_source.data_ptr();
+    std::vector<std::uint8_t> padded_bool;
+    if (upload_nbytes != nbytes) {
+      padded_bool.resize(upload_nbytes, 0);
+      std::memcpy(padded_bool.data(), upload_data, nbytes);
+      upload_data = padded_bool.data();
+    }
+    core::getWebGPUContext().getQueue().WriteBuffer(
+        destination_allocation.buffer,
+        destination_offset,
+        upload_data,
+        upload_nbytes);
+    return destination;
+  }
+
+  if (source.device().is_privateuseone() &&
+      destination.device().is_privateuseone()) {
+    check_supported_tensor(source, "WebGPU copy");
+    check_supported_tensor(destination, "WebGPU copy");
+    TORCH_CHECK(
+        source.scalar_type() == destination.scalar_type(),
+        "WebGPU copy requires matching dtypes");
+    TORCH_CHECK(source.sizes() == destination.sizes(), "copy shape mismatch");
+    const auto nbytes = checked_nbytes(source);
+    if (nbytes == 0) {
+      return destination;
+    }
+    auto& source_allocation = allocation(source);
+    auto& destination_allocation = allocation(destination);
+    const auto source_offset = checked_u32(
+        source.storage_offset() * source.element_size(),
+        "source byte offset");
+    const auto destination_offset = checked_u32(
+        destination.storage_offset() * destination.element_size(),
+        "destination byte offset");
+    if (source.scalar_type() == at::kBool) {
+      TORCH_CHECK(
+          source_offset % 4 == 0 && destination_offset % 4 == 0 &&
+              nbytes % 4 == 0,
+          "direct WebGPU Bool copy requires four-byte-aligned offsets and "
+          "length; use clone() or _to_copy for an arbitrary Bool view");
+    }
+    TORCH_CHECK(
+        static_cast<std::uint64_t>(source_offset) + nbytes <=
+            source_allocation.nbytes,
+        "WebGPU copy exceeds source storage");
+    TORCH_CHECK(
+        static_cast<std::uint64_t>(destination_offset) + nbytes <=
+            destination_allocation.nbytes,
+        "WebGPU copy exceeds destination storage");
+    if (source_allocation.buffer.Get() == destination_allocation.buffer.Get()) {
+      TORCH_CHECK(
+          source_offset == destination_offset,
+          "WebGPU copy between different views of the same GPUBuffer is not "
+          "supported");
+      return destination;
+    }
+    auto encoder =
+        core::getWebGPUContext().getDevice().CreateCommandEncoder();
+    encoder.CopyBufferToBuffer(
+        source_allocation.buffer,
+        source_offset,
+        destination_allocation.buffer,
+        destination_offset,
+        nbytes);
+    auto command_buffer = encoder.Finish();
+    core::getWebGPUContext().getQueue().Submit(1, &command_buffer);
+    return destination;
+  }
+
+  if (source.device().is_privateuseone() && destination.device().is_cpu()) {
+    TORCH_CHECK(
+        false,
+        "browser WebGPU readback is asynchronous; use "
+        "`await torch.webgpu.to_cpu_async(tensor)` instead of `.cpu()`");
+  }
+
+  TORCH_CHECK(
+      false,
+      "unsupported WebGPU copy: ",
+      source.device(),
+      " -> ",
+      destination.device());
+}
+
+void initialize() {
+  core::getWebGPUContext().initialize();
+}
+
+void batch_begin() {
+  TORCH_CHECK(
+      core::getWebGPUContext().is_initialized(),
+      "WebGPU is not initialized; call `torch.webgpu.init()` first");
+  torch_webgpu_batch_begin();
+}
+
+void batch_end() {
+  TORCH_CHECK(
+      torch_webgpu_batch_end() >= 0,
+      "unbalanced WebGPU batch scope");
+}
+
+void flush() {
+  TORCH_CHECK(
+      core::getWebGPUContext().is_initialized(),
+      "WebGPU is not initialized; call `torch.webgpu.init()` first");
+  torch_webgpu_flush();
+}
+
+std::int64_t buffer_id(const at::Tensor& tensor) {
+  if (tensor.numel() == 0) {
+    return 0;
+  }
+  return static_cast<std::int64_t>(reinterpret_cast<std::uintptr_t>(
+      allocation(tensor).buffer.Get()));
+}
+
+std::int64_t buffer_nbytes(const at::Tensor& tensor) {
+  return checked_nbytes(tensor);
+}
+
+} // namespace
+
+C10_REGISTER_GUARD_IMPL(PrivateUse1, WebGPUGuardImpl);
+
+TORCH_LIBRARY_IMPL(aten, PrivateUse1, module) {
+  module.impl("empty.memory_format", TORCH_FN(empty_memory_format));
+  module.impl("empty_strided", TORCH_FN(empty_strided));
+  module.impl("as_strided", TORCH_FN(as_strided));
+  module.impl("view", TORCH_FN(view));
+  module.impl("_copy_from", TORCH_FN(copy_from));
+}
+
+TORCH_LIBRARY(webgpu, module) {
+  module.def("initialize() -> ()");
+  module.def("_batch_begin() -> ()");
+  module.def("_batch_end() -> ()");
+  module.def("_flush() -> ()");
+  module.def("buffer_id(Tensor tensor) -> int");
+  module.def("buffer_nbytes(Tensor tensor) -> int");
+}
+
+TORCH_LIBRARY_IMPL(webgpu, CatchAll, module) {
+  module.impl("initialize", TORCH_FN(initialize));
+  module.impl("_batch_begin", TORCH_FN(batch_begin));
+  module.impl("_batch_end", TORCH_FN(batch_end));
+  module.impl("_flush", TORCH_FN(flush));
+}
+
+TORCH_LIBRARY_IMPL(webgpu, PrivateUse1, module) {
+  module.impl("buffer_id", TORCH_FN(buffer_id));
+  module.impl("buffer_nbytes", TORCH_FN(buffer_nbytes));
+}
+
+TORCH_LIBRARY_IMPL(_, AutogradPrivateUse1, module) {
+  module.fallback(torch::CppFunction::makeFallthrough());
+}
+
+} // namespace torch_webgpu::browser
diff --git a/torch/csrc/webgpu/webgpu_context.cpp b/torch/csrc/webgpu/webgpu_context.cpp
new file mode 100644
index 0000000..23bdf1a
--- /dev/null
+++ b/torch/csrc/webgpu/webgpu_context.cpp
@@ -0,0 +1,44 @@
+#include "core/webgpu_context.h"
+
+#include <c10/util/Exception.h>
+
+#include <cstdint>
+
+namespace torch_webgpu::core {
+namespace {
+
+constexpr std::uintptr_t kBrowserDeviceHandle = 1;
+constexpr std::uintptr_t kBrowserQueueHandle = 2;
+
+} // namespace
+
+void WebGPUContext::initialize() {
+  if (is_initialized()) {
+    return;
+  }
+  device_ = wgpu::Device::Acquire(
+      reinterpret_cast<WGPUDevice>(kBrowserDeviceHandle));
+  queue_ = wgpu::Queue::Acquire(
+      reinterpret_cast<WGPUQueue>(kBrowserQueueHandle));
+}
+
+bool WebGPUContext::is_initialized() const {
+  return device_ != nullptr && queue_ != nullptr;
+}
+
+wgpu::Device WebGPUContext::getDevice() const {
+  TORCH_CHECK(is_initialized(), "WebGPU native context is not initialized");
+  return device_;
+}
+
+wgpu::Queue WebGPUContext::getQueue() const {
+  TORCH_CHECK(is_initialized(), "WebGPU native context is not initialized");
+  return queue_;
+}
+
+WebGPUContext& getWebGPUContext() {
+  static WebGPUContext context;
+  return context;
+}
+
+} // namespace torch_webgpu::core
diff --git a/torch/webgpu/__init__.py b/torch/webgpu/__init__.py
new file mode 100644
index 0000000..4845eb3
--- /dev/null
+++ b/torch/webgpu/__init__.py
@@ -0,0 +1,485 @@
+"""Experimental browser WebGPU backend for the Pyodide PyTorch build.
+
+GPU storage and ATen dispatch are implemented in C++ using PyTorch's
+``PrivateUse1`` extension points. Browser WebGPU commands are issued through
+an Emscripten JavaScript bridge embedded in ``torch._C``. GPU work is enqueued
+synchronously. WebGPU buffer mapping remains asynchronous, but Pyodide's
+JSPI bridge can expose synchronous ``Tensor.cpu()``, ``Tensor.item()``, and
+scalar truth testing while Python is entered through a stack-switching-capable
+API.
+"""
+
+from __future__ import annotations
+
+from collections.abc import Iterator
+from contextlib import contextmanager
+import sys
+from typing import Any
+
+import torch
+
+_original_tensor_bool = torch.Tensor.__bool__
+_original_tensor_cpu = torch.Tensor.cpu
+_original_tensor_item = torch.Tensor.item
+
+_adapter: Any | None = None
+_device: Any | None = None
+
+
+def _js() -> Any:
+    try:
+        import js
+    except ImportError as exc:  # pragma: no cover - browser-only module
+        raise RuntimeError("the WebGPU backend requires Pyodide") from exc
+    return js
+
+
+def _js_object(**values: Any) -> Any:
+    result = _js().Object.new()
+    for key, value in values.items():
+        setattr(result, key, value)
+    return result
+
+
+def _js_array(*values: Any) -> Any:
+    result = _js().Array.new()
+    for value in values:
+        result.push(value)
+    return result
+
+
+def _state() -> Any:
+    state = getattr(_js().globalThis, "__torchWebGPU", None)
+    if state is None:
+        raise RuntimeError(
+            "WebGPU is not initialized; call `torch.webgpu.init()` first"
+        )
+    return state
+
+
+async def init_async(*, device: Any | None = None) -> None:
+    """Asynchronously initialize the browser GPUDevice.
+
+    This is the portable fallback for runtimes or entrypoints without JSPI.
+    Code entered through ``runPythonAsync()`` or ``callPromising()`` should
+    normally use :func:`init`, which keeps the Python call graph synchronous.
+    """
+
+    global _adapter, _device
+
+    if is_initialized():
+        return
+    if device is None:
+        gpu = getattr(_js().navigator, "gpu", None)
+        if not gpu:
+            raise RuntimeError("WebGPU is not exposed by this browser/runtime")
+        _adapter = await gpu.requestAdapter(
+            _js_object(powerPreference="high-performance")
+        )
+        # Pyodide 314 represents JavaScript null as the falsey `jsnull`
+        # singleton rather than Python None.
+        if not _adapter:
+            raise RuntimeError("the browser did not provide a WebGPU adapter")
+        required_features = _js_array()
+        if _adapter.features.has("subgroups"):
+            required_features.push("subgroups")
+        device = await _adapter.requestDevice(
+            _js_object(requiredFeatures=required_features)
+        )
+
+    objects = _js().Map.new()
+    buffers = _js().Map.new()
+    pending_command_buffers = _js().Array.new()
+    pending_buffer_uses = _js().Set.new()
+    deferred_buffer_releases = _js().Set.new()
+    state = _js_object(
+        device=device,
+        queue=device.queue,
+        objects=objects,
+        buffers=buffers,
+        pendingCommandBuffers=pending_command_buffers,
+        pendingBufferUses=pending_buffer_uses,
+        deferredBufferReleases=deferred_buffer_releases,
+        nextObjectId=3,
+        batchDepth=0,
+        batchCommandEncoder=None,
+        batchCommandBuffer=None,
+        bufferAllocations=0,
+        bufferReleases=0,
+        resourceReleases=0,
+        uploads=0,
+        gpuCopies=0,
+        shaderCompilations=0,
+        pipelineCreations=0,
+        commandEncoderCreations=0,
+        commandBufferCreations=0,
+        commandSubmissions=0,
+        commandBuffersSubmitted=0,
+        physicalCommandBuffersSubmitted=0,
+        batchedCommandBuffers=0,
+        batchFlushes=0,
+        dispatches=0,
+        readbacks=0,
+        failedDispatches=0,
+    )
+    objects.set(
+        1,
+        _js_object(
+            value=device, type="device", refs=1, imported=True
+        ),
+    )
+    objects.set(
+        2,
+        _js_object(
+            value=device.queue, type="queue", refs=1, imported=True
+        ),
+    )
+    setattr(_js().globalThis, "__torchWebGPU", state)
+    try:
+        torch.ops.webgpu.initialize()
+    except BaseException:
+        setattr(_js().globalThis, "__torchWebGPU", None)
+        raise
+    _device = device
+
+
+def _run_sync(
+    awaitable_factory: Any,
+    *,
+    operation: str,
+    async_fallback: str,
+):
+    """Run one browser awaitable through Pyodide's active JSPI stack."""
+
+    try:
+        from pyodide.ffi import can_run_sync, run_sync
+    except ImportError as exc:  # pragma: no cover - browser-only module
+        raise RuntimeError(f"{operation} requires Pyodide JSPI") from exc
+    if not can_run_sync():
+        raise RuntimeError(
+            f"{operation} requires Pyodide JSPI and a Python entrypoint "
+            "using runPythonAsync() or callPromising(); use "
+            f"`{async_fallback}` instead"
+        )
+    return run_sync(awaitable_factory())
+
+
+def init(*, device: Any | None = None) -> None:
+    """Initialize WebGPU without an exposed Python ``await`` boundary.
+
+    JavaScript must enter Python through ``runPythonAsync()`` or invoke a
+    Python function through ``callPromising()`` so Pyodide can stack-switch.
+    Use :func:`init_async` when JSPI is unavailable.
+    """
+
+    if is_initialized():
+        return
+    _run_sync(
+        lambda: init_async(device=device),
+        operation="synchronous WebGPU initialization",
+        async_fallback="await torch.webgpu.init_async(...)",
+    )
+
+
+@contextmanager
+def batch() -> Iterator[None]:
+    """Batch eager WebGPU command buffers into one explicit submission scope.
+
+    Scopes may be nested; only the outer exit submits pending work. A
+    synchronization, readback, or an order-dependent CPU-to-GPU write can
+    flush pending commands without ending the scope. Work outside a scope
+    keeps the eager one-command-buffer-per-submit behavior.
+    """
+
+    _state()
+    torch.ops.webgpu._batch_begin()
+    try:
+        yield
+    finally:
+        torch.ops.webgpu._batch_end()
+
+
+def _flush() -> None:
+    _state()
+    torch.ops.webgpu._flush()
+
+
+async def synchronize_async() -> None:
+    """Asynchronously wait for commands submitted before this call."""
+
+    _flush()
+    await _state().queue.onSubmittedWorkDone()
+
+
+def synchronize() -> None:
+    """Wait for queued WebGPU work through an active JSPI stack."""
+
+    _run_sync(
+        synchronize_async,
+        operation="synchronous WebGPU synchronization",
+        async_fallback="await torch.webgpu.synchronize_async()",
+    )
+
+
+async def to_cpu_async(tensor: torch.Tensor) -> torch.Tensor:
+    """Read a supported WebGPU tensor, materializing views on the GPU."""
+
+    import numpy as np
+
+    if tensor.device.type != "webgpu":
+        raise TypeError(f"expected a WebGPU tensor, got {tensor.device}")
+    if tensor.dtype not in (torch.float32, torch.int32, torch.long, torch.bool):
+        raise NotImplementedError(
+            "WebGPU readback supports only torch.float32, torch.int32, "
+            "torch.int64, and torch.bool"
+        )
+    if not tensor.is_contiguous():
+        tensor = tensor.contiguous()
+    _flush()
+    nbytes = int(torch.ops.webgpu.buffer_nbytes(tensor))
+    if nbytes == 0:
+        return torch.empty(tuple(tensor.shape), dtype=tensor.dtype)
+
+    state = _state()
+    identifier = int(torch.ops.webgpu.buffer_id(tensor))
+    source = state.buffers.get(identifier)
+    if source is None:
+        raise RuntimeError("WebGPU tensor refers to a released GPUBuffer")
+
+    logical_source_offset = int(tensor.storage_offset()) * tensor.element_size()
+    source_offset = logical_source_offset & ~3
+    readback_prefix = logical_source_offset - source_offset
+    allocated_nbytes = (readback_prefix + nbytes + 3) & ~3
+    if source_offset + allocated_nbytes > int(source.size):
+        raise RuntimeError("WebGPU readback view exceeds its GPUBuffer storage")
+    readback = state.device.createBuffer(
+        _js_object(
+            label="PyTorch WebGPU readback",
+            size=allocated_nbytes,
+            usage=0x0001 | 0x0008,
+        )
+    )
+    encoder = state.device.createCommandEncoder(
+        _js_object(label="PyTorch WebGPU readback")
+    )
+    state.commandEncoderCreations += 1
+    encoder.copyBufferToBuffer(
+        source, source_offset, readback, 0, allocated_nbytes
+    )
+    command_buffer = encoder.finish()
+    state.commandBufferCreations += 1
+    state.queue.submit(_js_array(command_buffer))
+    state.commandSubmissions += 1
+    state.commandBuffersSubmitted += 1
+    state.physicalCommandBuffersSubmitted += 1
+    state.readbacks += 1
+    await readback.mapAsync(_js().GPUMapMode.READ)
+    try:
+        mapped = readback.getMappedRange(0, allocated_nbytes)
+        byte_view = _js().Uint8Array.new(mapped)
+        numpy_dtype = {
+            torch.float32: np.float32,
+            torch.int32: np.int32,
+            torch.long: np.int64,
+            torch.bool: np.bool_,
+        }[tensor.dtype]
+        array = np.frombuffer(
+            byte_view.to_py(),
+            dtype=numpy_dtype,
+            count=tensor.numel(),
+            offset=readback_prefix,
+        ).copy()
+    finally:
+        readback.unmap()
+        readback.destroy()
+    return torch.frombuffer(array, dtype=tensor.dtype).clone().reshape(
+        tuple(tensor.shape)
+    )
+
+
+def to_cpu_sync(tensor: torch.Tensor) -> torch.Tensor:
+    """Synchronously read a WebGPU tensor when Pyodide JSPI is active.
+
+    Browser code must enter Python through ``runPythonAsync()`` or call a
+    Python function through ``callPromising()``. Runtimes without JSPI, and
+    synchronous Python entrypoints, must use :func:`to_cpu_async` instead.
+    """
+
+    if tensor.device.type != "webgpu":
+        raise TypeError(f"expected a WebGPU tensor, got {tensor.device}")
+    return _run_sync(
+        lambda: to_cpu_async(tensor),
+        operation="synchronous WebGPU readback",
+        async_fallback="await torch.webgpu.to_cpu_async(tensor)",
+    )
+
+
+def _tensor_cpu(
+    tensor: torch.Tensor,
+    memory_format: torch.memory_format = torch.preserve_format,
+) -> torch.Tensor:
+    if tensor.device.type != "webgpu":
+        return _original_tensor_cpu(tensor, memory_format=memory_format)
+    return to_cpu_sync(tensor)
+
+
+def _tensor_item(tensor: torch.Tensor) -> Any:
+    if tensor.device.type != "webgpu":
+        return _original_tensor_item(tensor)
+    return _original_tensor_item(to_cpu_sync(tensor))
+
+
+def _tensor_bool(tensor: torch.Tensor) -> bool:
+    if tensor.device.type != "webgpu":
+        return _original_tensor_bool(tensor)
+    # Preserve Tensor.__bool__ cardinality and ambiguity errors after the
+    # only browser-safe synchronization boundary. This avoids registering a
+    # fake synchronous `_local_scalar_dense` C++ kernel.
+    return _original_tensor_bool(to_cpu_sync(tensor))
+
+
+def is_initialized() -> bool:
+    try:
+        state = getattr(_js().globalThis, "__torchWebGPU", None)
+        return (
+            state is not None
+            and getattr(state, "device", None) is not None
+            and getattr(state, "queue", None) is not None
+        )
+    except RuntimeError:
+        return False
+
+
+def is_available() -> bool:
+    if is_initialized():
+        return True
+    try:
+        return bool(getattr(_js().navigator, "gpu", None))
+    except RuntimeError:
+        return False
+
+
+def current_device() -> int:
+    return 0
+
+
+def device_count() -> int:
+    return 1 if is_available() else 0
+
+
+def get_amp_supported_dtype() -> list[torch.dtype]:
+    return [torch.float32]
+
+
+def _is_in_bad_fork() -> bool:
+    return False
+
+
+def manual_seed_all(seed: int) -> None:
+    del seed
+
+
+def kernel_submissions() -> int:
+    return int(_state().dispatches)
+
+
+def copy_submissions() -> int:
+    return int(_state().gpuCopies)
+
+
+def shader_compilations() -> int:
+    return int(_state().shaderCompilations)
+
+
+def pipeline_creations() -> int:
+    return int(_state().pipelineCreations)
+
+
+def command_submissions() -> int:
+    return int(_state().commandSubmissions)
+
+
+def command_buffers_submitted() -> int:
+    return int(_state().commandBuffersSubmitted)
+
+
+def readback_submissions() -> int:
+    return int(_state().readbacks)
+
+
+def diagnostics() -> dict[str, int]:
+    """Return browser-transport counters used to detect accidental fallback."""
+
+    state = _state()
+    return {
+        "buffer_allocations": int(state.bufferAllocations),
+        "buffer_releases": int(state.bufferReleases),
+        "resource_releases": int(state.resourceReleases),
+        "uploads": int(state.uploads),
+        "gpu_copies": int(state.gpuCopies),
+        "shader_compilations": int(state.shaderCompilations),
+        "pipeline_creations": int(state.pipelineCreations),
+        "command_encoder_creations": int(state.commandEncoderCreations),
+        "command_buffer_creations": int(state.commandBufferCreations),
+        "command_submissions": int(state.commandSubmissions),
+        "command_buffers_submitted": int(state.commandBuffersSubmitted),
+        "physical_command_buffers_submitted": int(
+            state.physicalCommandBuffersSubmitted
+        ),
+        "batched_command_buffers": int(state.batchedCommandBuffers),
+        "batch_flushes": int(state.batchFlushes),
+        "batch_depth": int(state.batchDepth),
+        "pending_command_buffers": int(state.pendingCommandBuffers.length),
+        "dispatches": int(state.dispatches),
+        "readbacks": int(state.readbacks),
+        "failed_dispatches": int(state.failedDispatches),
+        "cpu_fallbacks": 0,
+    }
+
+
+def cpu_fallbacks() -> int:
+    """Return the number of implicit CPU fallbacks.
+
+    This initial backend does not register any CPU fallback path; unsupported
+    operations fail instead of silently moving tensor data off the GPU.
+    """
+
+    return 0
+
+
+torch.utils.rename_privateuse1_backend("webgpu")
+torch._register_device_module("webgpu", sys.modules[__name__])
+torch.utils.generate_methods_for_privateuse1_backend(
+    for_tensor=True,
+    for_module=True,
+    for_storage=False,
+    for_packed_sequence=True,
+)
+torch.Tensor.__bool__ = _tensor_bool
+torch.Tensor.cpu = _tensor_cpu
+torch.Tensor.item = _tensor_item
+
+
+__all__ = [
+    "batch",
+    "command_buffers_submitted",
+    "command_submissions",
+    "copy_submissions",
+    "cpu_fallbacks",
+    "current_device",
+    "diagnostics",
+    "device_count",
+    "get_amp_supported_dtype",
+    "init",
+    "init_async",
+    "is_available",
+    "is_initialized",
+    "kernel_submissions",
+    "pipeline_creations",
+    "readback_submissions",
+    "shader_compilations",
+    "synchronize",
+    "synchronize_async",
+    "to_cpu_async",
+    "to_cpu_sync",
+]
-- 
2.51.1
